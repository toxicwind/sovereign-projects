// trace_gpu.bt: Measure time from GPU interrupt to process signal
kprobe:nv_kern_isr {
    @start[tid] = nsecs;
}

kretprobe:nv_kern_isr {
    if (@start[tid]) {
        @latency = hist(nsecs - @start[tid]);
        delete(@start[tid]);
    }
}