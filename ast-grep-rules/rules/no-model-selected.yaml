id: no-model-selected
language: ts
severity: warning
message: "Unresolved 'No model selected' emitter — step 2 of sovereign TODO not finalized."
rule:
  any:
    - pattern: ctx.ui.notify("No model selected", $KIND)
    - pattern: throw new Error("No model selected" + $REST)
    - pattern: return `No model selected.$REST`
