# squawk-feed settings - KEY=VALUE lines (2026-09-21).
# Loaded at startup; real environment variables always win.
# Missing file is fine. Runtime-local (0600), NOT tracked in git.
# Tracked example: projects/range/ranch/squawk/settings.conf.example
#
# Future keys go here, one per line, e.g.:
# SQUAWK_FEED_EXTRA_KEY=...
