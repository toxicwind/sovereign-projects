---
name: nim-refresh
description: Refresh nimstats-derived model picks and report the winners.
---
Call `nimstats_query({role:"fast"})`, `nimstats_query({role:"smart"})`,
`nimstats_query({role:"plan"})` and print the three best models.
If the local Nanocoder default differs, note it and suggest a switch.
