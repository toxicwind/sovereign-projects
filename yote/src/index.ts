/**
 * src/index.ts
 * Yote entry point — simply boots the full orchestrator.
 * All logic lives in src/yote.ts (merged core).
 *
 * Run with:
 *   bun run src/index.ts
 *   or `bun run start`
 */

import "./yote.ts";
