#!/usr/bin/env bash
# MISE description="Core stack: llama-herder + openfang + prometheus"
set -euo pipefail
source "$(dirname "$0")/_lib.sh"

mapfile -d '' -t cfg < <(pc_args core)
exec run_pc up "${cfg[@]}" -D "$@"
