#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/_lib.sh"
# Daily driver: core + edge proxy + telegram bot + rust dashboard
"$SOV/stack/build-compose.sh" >/dev/null
pc_up llama-swap openfang prometheus null-g-proxy yote rust-web
