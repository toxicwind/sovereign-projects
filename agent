#!/usr/bin/env bash
set -euo pipefail

export SOVEREIGN_HOME="/home/toxic/projects/sovereign-projects/tau"
export PI_CONFIG_DIR="${PI_CONFIG_DIR:-$HOME/.tau}"
export PI_CODING_AGENT_DIR="${PI_CODING_AGENT_DIR:-$HOME/.tau/agent}"
export DAEMON_PROJECT_DIR="${DAEMON_PROJECT_DIR:-$HOME}"
export DAEMON_RUNTIME_DIR="${DAEMON_RUNTIME_DIR:-$HOME/.tau/run/daemons/00a0a40d4f605f26}"

if [ -f "$HOME/.secrets" ]; then
    set -a
    source "$HOME/.secrets" 2>/dev/null || true
    set +a
fi

exec /home/toxic/.bun/bin/bun run "${SOVEREIGN_HOME}/packages/coding-agent/src/cli.ts" -- "$@"
