# Tally — memory
- 2026-09-21: tau-routing scorekeeper lane; holding the tally-sidechat workstream lock.
- Task in flight: "Find where 8420 moved" — the port went missing; hunting its new location.
- Lane rule: every routing claim needs measured evidence — latency numbers, probe results — or it doesn't ship.
