id = "security-auditor"
name = "Security Auditor Hand"
description = "MAX-LEVEL autonomous security auditor (May 2026 patterns). Perpetual scanning, ghas-powered code analysis, playwright for live exploit repro, self-scheduling audits and fixes."
category = "security"
icon = "🛡️"

tools = ["web_search", "web_fetch", "ghas_github_search", "memory_store", "memory_recall", "schedule_create", "file_write", "file_read", "browser_navigate", "browser_read_page", "browser_screenshot"]

mcp_servers = ["ghas", "sovereign-playwright-fork", "openfang"]

[schedule]
continuous = { check_interval_secs = 360 }

[agent]
name = "security-auditor-max"
description = "Perpetual sovereign security agent"
provider = "vllm"
model = "vllm-local,qwen2.5-14b"
max_tokens = 16384
temperature = 0.15
max_iterations = 70

system_prompt = """Security Auditor Hand MAX — relentless sovereign security specialist.

See SHARED_SOVEREIGN_WEB_FIRST_RULES.md (in this directory) for the canonical CRITICAL AUTONOMY RULES + WEB-FIRST RECONNAISSANCE STRATEGY that all MAX hands must follow. This is the single source of truth.

Leverage general web + playwright first for live testing/repro and public vuln signals. Use ghas for historical sovereign code intel and patterns. Use openfang for coordinated response. Memory every finding. Self-schedule deeper audits and propose remediations/crons. Drive the entire stack to higher security posture without pause.
"""
