# Bashrc Full Verification — 2026-08-11T14:59:58Z

## Architecture Summary

```
~/.bashrc                          # Router entry point
├── _sov_detect_profile()          # Priority: PI_AGENT > SSH > WezTerm > VSCode > default
├── source ~/.bashrc.d/core.sh     # Core env, PATH, secrets (all shells)
├── source ~/.bashrc.d/common/     # grok, grok-upstream, pi, aliases (all shells)
├── source ~/.bashrc.d/<profile>/  # Profile-specific config (all shells)
│   ├── pi-agent/00-pi-config.sh   # PI config, debug, no starship, green PS1
│   ├── user-ssh/00-starship.sh
│   ├── user-wezterm/00-starship.sh
│   ├── user-vscode/00-starship.sh
│   └── user-local/00-starship.sh
└── if non-interactive: return 0   # Early exit
    source ~/.bashrc.d/interactive.sh  # carapace, mise, zoxide, fzf, direnv, atuin, starship
```

## Test Results

### 1. Profile Detection (clean env)

| Environment | Expected | Actual | Pass |
|-------------|----------|--------|------|
| clean env | user-local | user-local | ✅ |
| TERM_PROGRAM=WezTerm | user-wezterm | user-wezterm | ✅ |
| SSH_CONNECTION=1 | user-ssh | user-ssh | ✅ |
| TERM_PROGRAM=vscode VSCODE_PID=1 | user-vscode | user-vscode | ✅ |
| PI_AGENT=1 | pi-agent | pi-agent | ✅ |

With PI_AGENT=1:
- SOV_PROFILE=pi-agent ✅
- PI_CONFIG_PATH=/home/toxic/.pi/agent/config.yaml ✅
- PI_AGENT_DIR=/home/toxic/.pi/agent ✅
- PS1=\[\e[32m\]pi-agent\[\e[0m\] \w \$  ✅

### 2. Function Availability (non-interactive)

All functions available in non-interactive shells:
- `grok` → fork binary (`xai-grok-pager`)
- `grok-upstream` → upstream binary (`~/.grok/downloads/grok-1.0.0-linux-x86_64`)
- `pi` → pi-agent with `PI_START_PWD=/home/toxic`

### 3. Binary Execution

```
grok --version:
  [grok] toxicwind/grok-build@1f51e3a — fork of xai-org/grok-build
  grok 1.0.0 (1f51e3a) [stable]

grok-upstream --version:
  grok 1.0.0 (3cd0d0cbce) [stable]
```

### 4. Fork Metadata

- **Commit**: 1f51e3a5846de8b7c67f1216196222f91769511c — Merge remote-tracking branch 'upstream/main'
- **Upstream diff**: 2 commits
  - 1f51e3a Merge remote-tracking branch 'upstream/main'
  - 7c68e8b docs: add serialization error fix documentation

### 5. Module Structure

```
/home/toxic/.bashrc.d/
├── core.sh
├── interactive.sh
├── common/
│   └── 00-functions.sh
├── pi-agent/
│   └── 00-pi-config.sh
├── user-local/
│   └── 00-starship.sh
├── user-ssh/
│   └── 00-starship.sh
├── user-vscode/
│   └── 00-starship.sh
└── user-wezterm/
    └── 00-starship.sh
```

---

## Status: ALL TESTS PASS ✅