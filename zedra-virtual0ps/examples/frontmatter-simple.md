---
title: Markdown Frontmatter Example
description: >
  Verifies that frontmatter is rendered as a metadata block, even when the
  metadata includes nested objects and multiline values.
tags:
  - markdown
  - frontmatter
draft: true
status:
  published: false
  archived: false
aliases:
  - markdown-frontmatter
  - preview-frontmatter
---

# Markdown Frontmatter Example

The preview should render the metadata block above this heading.

## Verification

- Open this file in the workspace Markdown view.
- Confirm the frontmatter renders as a metadata block.
- Select this list and add it to chat.
- Confirm the attached source range uses the original line numbers after the frontmatter.
