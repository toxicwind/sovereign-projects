# MANAGED SOURCE — port-dynamic 2026-09-20.
# This file is the canonical nginx config for the pitchfork nginx daemon.
# /etc/nginx/nginx.conf is the legacy path (kept in sync, same content).
user http;
error_log /home/toxic/.local/share/nginx/error.log;
events { worker_connections 16384; }
http {
    access_log /home/toxic/.local/share/nginx/access.log;
    server {
        listen 25208;
        # 2026-09-14 tau-repair: DISABLED — backend :62201 dead, no code references this port
        # location /scanner/ { proxy_pass http://127.0.0.1:62201/; }
        # 2026-09-14 tau-repair: DISABLED — backend :62010 dead, no code references this port
        # location /agent/ { proxy_pass http://127.0.0.1:62010/; }
        location /crypto/ { alias /home/toxic/mist-factory/dashboard/; index index.html; }
    }
}
