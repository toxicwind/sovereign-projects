# projects

Reserved project boundary — intentionally empty.

Active workspaces live at the repository top level: `herd/`, `mesh/`, `tau/`, `yote/`, `openfang/`, `qed/`, `shell/`.

(The old `projects` gitlink was retired; this directory remains as a placeholder so historical paths don't break.)
