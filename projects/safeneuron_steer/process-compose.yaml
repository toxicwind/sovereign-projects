# process-compose for SafeNeuron-Steer
version: "0.5"

processes:
  safeneuron-web:
    command: "cargo run --release"
    availability:
      restart: "always"
    env:
      - RUST_LOG=info
      - PORT=25201
      - MODEL_DIR=/home/toxic/models
