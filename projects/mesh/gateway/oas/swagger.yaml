components:
  schemas:
    config.ConcurrencyDefaults:
      description: |-
        ServerConcurrencyDefaults is scope (b) of FR-020: the blanket per-server
        default set inherited by every server that does not override a setting.
        Absent (the default) = no per-server limiting unless a server configures
        it explicitly. File/API-configured only — no env scheme (FR-022).
      properties:
        max_concurrent_requests:
          type: integer
        queue_size:
          type: integer
        queue_timeout:
          type: string
      type: object
    config.Config:
      properties:
        activity_cleanup_interval_min:
          description: 'Background cleanup interval in minutes (default: 60)'
          type: integer
        activity_max_records:
          description: 'Max records before pruning (default: 100000)'
          type: integer
        activity_max_response_size:
          description: 'Response truncation limit in bytes (default: 65536)'
          type: integer
        activity_max_size_mb:
          description: 'Max total activity-log size in MB before pruning oldest (default:
            256, 0=disabled)'
          type: integer
        activity_retention_days:
          description: Activity logging settings (RFC-003)
          type: integer
        aggregate_upstream_prompts:
          description: |-
            AggregateUpstreamPrompts, when true, aggregates every connected upstream
            server's advertised MCP prompts into mcpproxy's own prompts/list
            (exposed as "<server>__<prompt>"). OFF by default: users are safe by
            default and opt in deliberately. EnablePrompts still governs the built-in
            prompts + the prompts capability; this flag gates ONLY the upstream
            aggregation performed by RefreshPrompts. Hot-reloadable.
          type: boolean
        allow_private_registry_fetch:
          description: |-
            AllowPrivateRegistryFetch opts out of the registry SSRF guard (MCP-1076,
            CWE-918). By default (false) registry fetches refuse any host that is — or
            resolves to — a non-routable address (loopback, RFC1918/CGNAT private,
            link-local incl. the 169.254.169.254 cloud-metadata endpoint), so a
            malicious or typo'd registry source cannot turn the daemon into a
            request-forgery vector against internal services.

            This opt-out is BLANKET (all-or-nothing): setting it true disables the
            guard for EVERY non-routable range at once — loopback, RFC1918/CGNAT
            private, link-local AND the 169.254.169.254 cloud-metadata endpoint. There
            is no way to allow only loopback; enabling it for a localhost dev registry
            also re-opens the cloud-metadata SSRF vector. Set true ONLY when you
            intentionally run a trusted registry mirror on an internal/private address,
            ideally on a host with no cloud-metadata exposure. The change takes effect
            only on daemon (re)start or config reload.
          type: boolean
        allow_server_add:
          type: boolean
        allow_server_remove:
          type: boolean
        api_key:
          description: Security settings
          type: string
        call_tool_timeout:
          type: string
        check_server_repo:
          description: Repository detection settings
          type: boolean
        code_execution_max_parallel:
          description: 'Default concurrency for call_tools() batches (1-32, default:
            8)'
          type: integer
        code_execution_max_tool_calls:
          description: 'Max tool calls per execution (0 = unlimited, default: 0)'
          type: integer
        code_execution_pool_size:
          description: 'JavaScript runtime pool size (default: 10)'
          type: integer
        code_execution_timeout_ms:
          description: 'Timeout in milliseconds (default: 120000, max: 600000)'
          type: integer
        data_dir:
          type: string
        debug_search:
          type: boolean
        disable_management:
          type: boolean
        docker_isolation:
          $ref: '#/components/schemas/config.DockerIsolationConfig'
        docker_recovery:
          $ref: '#/components/schemas/config.DockerRecoveryConfig'
        enable_code_execution:
          description: Code execution settings
          type: boolean
        enable_prompts:
          description: Prompts settings
          type: boolean
        enable_socket:
          description: 'Enable Unix socket/named pipe for local IPC (default: true)'
          type: boolean
        enable_tray:
          description: 'Deprecated: EnableTray is unused and has no runtime effect.
            Kept for backward compatibility.'
          type: boolean
        environment:
          $ref: '#/components/schemas/secureenv.EnvConfig'
        features:
          $ref: '#/components/schemas/config.FeatureFlags'
        forward_proxy_env:
          description: |-
            ForwardProxyEnv opts in to forwarding the ambient HTTP(S)/ALL/NO/FTP proxy
            environment variables to spawned stdio upstream servers (MCP-2769). OFF by
            default: proxy URLs commonly embed credentials (http://user:pass@proxy), so
            forwarding them to every upstream is a credential-leak risk. When enabled,
            values are forwarded with their userinfo (credentials) redacted.
          type: boolean
        health_check_interval:
          description: |-
            Discovery & health-check cadence (spec 074, #608). Both are *Duration
            tri-state pointers: nil = inherit the built-in default; a pointer to 0s =
            the loop is disabled; a positive value = that interval. Defaults live only
            in the resolvers (ResolveHealthCheckInterval / ResolveToolDiscoveryInterval)
            so an unset key behaves exactly as before this feature (SC-005). Validated
            in Validate(): health-check ∈ {0} ∪ [5s,1h]; tool-discovery ∈ {0} ∪ [30s,24h].
          type: string
        http_idle_timeout:
          description: |-
            HTTPIdleTimeout caps how long an idle keep-alive connection is kept open.
            Unset = 180s. "0s" removes the dedicated idle deadline, but net/http then
            falls back to ReadTimeout — idle is fully unbounded only when
            http_read_timeout is also "0s". Requires a restart.
          type: string
        http_read_timeout:
          description: |-
            HTTPReadTimeout caps how long reading a whole request (headers + body)
            may take. Unset = 120s; "0s" disables it. Requires a restart.
          type: string
        http_write_timeout:
          description: |-
            HTTPWriteTimeout caps how long producing a whole response may take on
            non-streaming endpoints (REST, Web UI, health). Unset = 120s; "0s"
            disables it globally. MCP and SSE /events routes are exempt by design.
          type: string
        init_timeout:
          description: |-
            InitTimeout is the global default deadline for an upstream's MCP
            `initialize` handshake (MCP-3322 / GH #760). *Duration tri-state: nil =
            inherit the built-in 30s default; a positive value = that deadline. A
            per-server InitTimeout overrides this. Resolved by ResolveInitTimeout;
            validated to {0} ∪ [1s, 30m] in Validate(). Servers doing legitimate
            first-run warmup (cache/index build) before answering `initialize` can
            raise this so they are not killed mid-startup.
          type: string
        instructions:
          description: |-
            Instructions text returned in the MCP initialize response to guide AI agents.
            When empty, a built-in default is used that explains retrieve_tools workflow.
          type: string
        intent_declaration:
          $ref: '#/components/schemas/config.IntentDeclarationConfig'
        listen:
          type: string
        logging:
          $ref: '#/components/schemas/config.LogConfig'
        max_concurrent_requests:
          description: |-
            Concurrency limits (spec 093, GH #955). Scope (a) of FR-020: the GLOBAL
            AGGREGATE limiter — one proxy-wide cap on concurrently running upstream
            tool calls, with its own bounded wait queue. Tri-state pointers: absent =
            the limiter does not exist (default, zero behavior change); an explicit 0
            max also disables it; positive = that cap. This scope is NEVER a
            per-server inheritance source — per-server values come from
            ServerConcurrencyDefaults / the per-server overrides — but a server's
            effective concurrency is bounded by BOTH its own limiter and this one.
            Resolved by ResolveGlobalConcurrency; hot-reloadable; overridable via
            MCPPROXY_MAX_CONCURRENT_REQUESTS / _QUEUE_SIZE / _QUEUE_TIMEOUT (FR-022).
          type: integer
        max_result_size_chars:
          description: Advertised on every tool as `_meta.anthropic/maxResultSizeChars`;
            raises Claude Code's inline-response ceiling from 50k to up to 500k chars.
            Set to 0 to disable.
          type: integer
        mcpServers:
          items:
            $ref: '#/components/schemas/config.ServerConfig'
          type: array
          uniqueItems: false
        oauth_expiry_warning_hours:
          description: Health status settings
          type: number
        observability:
          $ref: '#/components/schemas/config.ObservabilityConfig'
        output_sanitisation:
          $ref: '#/components/schemas/config.OutputSanitisationConfig'
        output_validation:
          $ref: '#/components/schemas/config.OutputValidationConfig'
        profiles:
          description: |-
            Profiles are optional named, server-scoped views exposed at /mcp/p/<name>
            (Spec 057). Absent/empty is fully supported — /mcp is unchanged and configs
            without this key serialize byte-identically (SC-004).
          items:
            $ref: '#/components/schemas/config.ProfileConfig'
          type: array
          uniqueItems: false
        quarantine_enabled:
          description: |-
            QuarantineEnabled controls whether quarantine is active. It gates two
            things together:
              1. Server-level auto-quarantine for newly added servers (issue #370).
                 When true, servers added via the upstream_servers MCP tool or the
                 REST API default to quarantined=true; when false, they default to
                 quarantined=false. Explicit per-request values always win.
              2. Tool-level quarantine (Spec 032): per-tool SHA-256 approval of
                 tool descriptions/schemas.
            When nil (default), quarantine is enabled (secure by default). Set to
            explicit false to opt out of both. Per-server SkipQuarantine still
            applies for the tool-level check on individual servers.
          type: boolean
        queue_size:
          type: integer
        queue_timeout:
          type: string
        read_only_mode:
          type: boolean
        registries:
          description: Registries configuration for MCP server discovery
          items:
            $ref: '#/components/schemas/config.RegistryEntry'
          type: array
          uniqueItems: false
        registries_locked:
          description: |-
            RegistriesLocked is an enterprise stub knob (MCP-866): when true, runtime
            additions of custom registries (e.g. `registry add-source`, the REST/MCP
            add-source surface) are rejected so an administrator can pin the discovery
            sources. Built-in defaults are unaffected. Documented but otherwise inert
            beyond the add-source rejection.
          type: boolean
        require_mcp_auth:
          description: 'Require authentication on /mcp endpoint (default: false)'
          type: boolean
        reveal_secret_headers:
          description: |-
            RevealSecretHeaders, when true, disables the redaction of the
            secret-bearing server fields — sensitive header values (Authorization,
            X-API-Key, Cookie, …), env-var secrets, and URL query credentials — in
            responses from the `upstream_servers` MCP tool, the `/api/v1/servers`
            REST API, and the SSE event stream. It also lets URL secrets echoed
            into last_error / health.detail through unscrubbed.

            Default false — sensitive values are surfaced masked as
            `••••<last2> (<N> chars)` (error strings use `***REDACTED***`) so an
            MCP agent cannot read Bearer tokens / API keys / URL secrets out of
            another upstream's config (PR #425, issue #872). ${env:…}/${keyring:…}
            references are labels, not secrets, and pass through unchanged.

            The Web UI / macOS tray edit forms work without seeing the real
            values: PATCH /api/v1/servers/{id} deep-merges (omitted keys are
            preserved, see `headers_remove` / `env_remove` for explicit
            deletes), so clients compute a diff and only send the keys that
            actually changed. Redacted-but-unchanged values never round-trip
            — the backend keeps the real string. Set this to true if a
            downstream tool genuinely needs raw values in the response.
          type: boolean
        routing_mode:
          description: |-
            Routing mode (Spec 031): how MCP tools are exposed to clients
            Valid values: "retrieve_tools" (default), "direct", "code_execution"
          type: string
        security:
          $ref: '#/components/schemas/config.SecurityConfig'
        sensitive_data_detection:
          $ref: '#/components/schemas/config.SensitiveDataDetectionConfig'
        server_concurrency_defaults:
          $ref: '#/components/schemas/config.ConcurrencyDefaults'
        telemetry:
          $ref: '#/components/schemas/config.TelemetryConfig'
        tls:
          $ref: '#/components/schemas/config.TLSConfig'
        tokenizer:
          $ref: '#/components/schemas/config.TokenizerConfig'
        tool_discovery_interval:
          type: string
        tool_response_limit:
          type: integer
        tool_response_mode:
          description: |-
            Tool response mode (Spec 085): how retrieve_tools serializes results.
            Valid values: "" (= full), "full" (default: today's schema-bearing
            entries), "compact" (signature + first-sentence entries). Orthogonal to
            routing_mode — routing_mode selects the tool SURFACE, this selects the
            SERIALIZATION within the retrieve_tools surface. Serialization-only: it
            never affects the query, ranking, or result set. Hot-reloadable.
          type: string
        tool_response_session_risk_warning:
          description: |-
            ToolResponseSessionRiskWarning controls whether the prose `warning` field
            is included in the `session_risk` object returned by `retrieve_tools`.
            The structured fields (level, lethal_trifecta, has_open_world_tools, etc.)
            are always included. Default: false (quiet for LLM clients) — see issue #406.
            Most tools lack annotations, so the MCP-spec defaults treat them as fully
            permissive across all three risk axes, which makes the prose warning fire
            on almost every call and wastes tokens.
          type: boolean
        tools_limit:
          type: integer
        toon_min_savings_pct:
          description: |-
            ToonMinSavingsPct is the minimum byte-savings percentage (validated
            1-90; 0/unset → 15) the complete TOON emission (marker + hint + body)
            must achieve over the exact passthrough emission for adaptive mode to
            encode a block. Byte savings approximate token savings for the tabular
            payload class; the spec-083 profiler reports true token deltas.
            Global-only (no per-server override, FR-001).
          type: integer
        toon_output:
          description: |-
            ToonOutput selects the TOON encoding mode for call_tool_* result text
            blocks (spec 084): "off" (default — responses byte-identical to
            pre-feature behavior), "adaptive" (encode only tabular-uniform payloads
            that beat compact JSON by ToonMinSavingsPct), or "always"
            (benchmark/debug only — encodes every JSON-parseable block and can
            INCREASE token cost). Per-server override: ServerConfig.ToonOutput.
            Resolved by ResolveToonOutput; hot-reloadable.
          type: string
        top_k:
          description: 'Deprecated: TopK is superseded by ToolsLimit and has no runtime
            effect. Kept for backward compatibility.'
          type: integer
        tray_endpoint:
          description: Tray endpoint override (unix:// or npipe://)
          type: string
        trusted_hosts:
          description: |-
            TrustedHosts lists non-loopback Host header values accepted on loopback
            listeners (GH #898). DNS-rebinding protection rejects requests whose Host
            header is not a loopback address when mcpproxy listens on loopback; a
            reverse proxy (nginx → 127.0.0.1) forwarding the public domain in Host
            trips it. Entries are hostnames, case-insensitive; an entry without a
            port matches any port, with a port it must match exactly; a leading dot
            (".example.com") is a subdomain wildcard. The single entry "*" disables
            Host and Origin validation entirely. The same list also validates the
            Origin header when present (MCP spec DNS-rebinding defense). Empty
            (default) keeps full protection. Env override: MCPPROXY_TRUSTED_HOSTS
            (comma-separated).
          items:
            type: string
          type: array
          uniqueItems: false
        update_check:
          $ref: '#/components/schemas/config.UpdateCheckConfig'
      type: object
    config.CustomPattern:
      properties:
        category:
          description: Category (defaults to "custom")
          type: string
        keywords:
          description: Keywords to match (mutually exclusive with Regex)
          items:
            type: string
          type: array
          uniqueItems: false
        name:
          description: Unique identifier for this pattern
          type: string
        regex:
          description: Regex pattern (mutually exclusive with Keywords)
          type: string
        severity:
          description: 'Risk level: critical, high, medium, low'
          type: string
      type: object
    config.DeepScanConfig:
      description: |-
        DeepScan is the opt-in "deep scan" layer (Spec 077 US3). It subsumes the
        deprecated top-level scanner_fetch_package_source / scanner_disable_no_new_privileges
        keys (migrated on load) and gates the heavy Docker-based scanners + source
        extraction. Disabled by default (FR-006): only the deterministic in-process
        baseline scanner runs. A deep-scan failure NEVER changes the baseline verdict
        (FR-007/FR-008).
      properties:
        disable_no_new_privileges:
          description: |-
            DisableNoNewPrivileges, when true, omits the `--security-opt
            no-new-privileges` flag from scanner container runs (snap-docker/AppArmor
            escape hatch). Absorbs the deprecated top-level
            scanner_disable_no_new_privileges. Default false.
          type: boolean
        enabled:
          description: Enabled is the master opt-in for the heavy layer (FR-006).
            Default false.
          type: boolean
        fetch_package_source:
          description: |-
            FetchPackageSource controls whether the scanner fetches the PUBLISHED
            source of package-runner servers (npx/uvx) — without executing it — when
            no local source is available. Absorbs the deprecated top-level
            scanner_fetch_package_source. Default (nil) is ENABLED within deep scan.
          type: boolean
        scanners:
          description: |-
            Scanners optionally restricts which deep scanners may run under the
            umbrella (by scanner id). Empty ⇒ all enabled deep scanners are eligible.
          items:
            type: string
          type: array
          uniqueItems: false
      type: object
    config.DockerIsolationConfig:
      description: Docker isolation settings
      properties:
        cpu_limit:
          description: CPU limit for containers
          type: string
        default_images:
          additionalProperties:
            type: string
          description: Map of runtime type to Docker image
          type: object
        enable_cache_volume:
          description: 'Mount shared cache volumes for faster restarts (default: true)'
          type: boolean
        enabled:
          description: Global enable/disable for Docker isolation (legacy; superseded
            by Mode)
          type: boolean
        extra_args:
          description: Additional docker run arguments
          items:
            type: string
          type: array
          uniqueItems: false
        log_driver:
          description: 'Docker log driver (default: json-file)'
          type: string
        log_max_files:
          description: 'Maximum number of log files (default: 3)'
          type: string
        log_max_size:
          description: 'Maximum size of log files (default: 100m)'
          type: string
        memory_limit:
          description: Memory limit for containers
          type: string
        mode:
          description: 'Isolation mode: "docker" | "sandbox" | "none" (MCP-34.2).
            Unset per-server inherits the global mode; unset globally falls back to
            the legacy "enabled" flag (true ⇒ docker, false ⇒ none)'
          type: string
          x-enum-varnames:
          - IsolationModeDocker
          - IsolationModeSandbox
          - IsolationModeNone
        network_mode:
          description: 'Docker network mode (default: bridge)'
          type: string
        registry:
          description: Custom registry (defaults to docker.io)
          type: string
        timeout:
          description: Container startup timeout
          type: string
      type: object
    config.DockerRecoveryConfig:
      description: Docker recovery settings
      properties:
        enabled:
          description: 'Enable Docker recovery monitoring (default: true)'
          type: boolean
        max_retries:
          description: Maximum retry attempts (0 = unlimited)
          type: integer
        notify_on_failure:
          description: 'Show notification on recovery failure (default: true)'
          type: boolean
        notify_on_retry:
          description: 'Show notification on each retry (default: false)'
          type: boolean
        notify_on_start:
          description: 'Show notification when recovery starts (default: true)'
          type: boolean
        notify_on_success:
          description: 'Show notification on successful recovery (default: true)'
          type: boolean
        persistent_state:
          description: 'Save recovery state across restarts (default: true)'
          type: boolean
      type: object
    config.FeatureFlags:
      description: 'Deprecated: Features flags are unused and have no runtime effect.
        Kept for backward compatibility.'
      properties:
        enable_async_storage:
          type: boolean
        enable_caching:
          type: boolean
        enable_contract_tests:
          type: boolean
        enable_debug_logging:
          description: Development features
          type: boolean
        enable_docker_isolation:
          type: boolean
        enable_event_bus:
          type: boolean
        enable_health_checks:
          type: boolean
        enable_metrics:
          type: boolean
        enable_oauth:
          description: Security features
          type: boolean
        enable_observability:
          description: Observability features
          type: boolean
        enable_quarantine:
          type: boolean
        enable_runtime:
          description: Runtime features
          type: boolean
        enable_search:
          description: Storage features
          type: boolean
        enable_sse:
          type: boolean
        enable_tracing:
          type: boolean
        enable_tray:
          type: boolean
        enable_web_ui:
          description: UI features
          type: boolean
      type: object
    config.IntentDeclarationConfig:
      description: Intent declaration settings (Spec 018)
      properties:
        strict_server_validation:
          description: |-
            StrictServerValidation controls whether server annotation mismatches
            cause rejection (true) or just warnings (false).
            Default: true (reject mismatches)
          type: boolean
      type: object
    config.IsolationConfig:
      description: Per-server isolation settings
      properties:
        enabled:
          description: Enable Docker isolation for this server (nil = inherit global;
            legacy, superseded by Mode)
          type: boolean
        extra_args:
          description: Additional docker run arguments for this server
          items:
            type: string
          type: array
          uniqueItems: false
        image:
          description: Custom Docker image (overrides default)
          type: string
        log_driver:
          description: Docker log driver override for this server
          type: string
        log_max_files:
          description: Maximum number of log files override
          type: string
        log_max_size:
          description: Maximum size of log files override
          type: string
        mode:
          $ref: '#/components/schemas/config.IsolationMode'
        network_mode:
          description: Custom network mode for this server
          type: string
        working_dir:
          description: Custom working directory in container
          type: string
      type: object
    config.IsolationMode:
      description: 'Isolation mode: "docker" | "sandbox" | "none" (MCP-34.2). Unset
        per-server inherits the global mode; unset globally falls back to the legacy
        "enabled" flag (true ⇒ docker, false ⇒ none)'
      type: string
      x-enum-varnames:
      - IsolationModeDocker
      - IsolationModeSandbox
      - IsolationModeNone
    config.LogConfig:
      description: Logging configuration
      properties:
        compress:
          type: boolean
        enable_console:
          type: boolean
        enable_file:
          type: boolean
        filename:
          type: string
        json_format:
          type: boolean
        level:
          type: string
        log_dir:
          description: Custom log directory
          type: string
        max_age:
          description: days
          type: integer
        max_backups:
          description: number of backup files
          type: integer
        max_size:
          description: MB
          type: integer
      type: object
    config.MetricsExporterConfig:
      description: |-
        Metrics gates the Prometheus /metrics scrape endpoint (MCP-32). Disabled
        by default — operators opt in for k8s/enterprise deployments.
      properties:
        enabled:
          description: Enabled exposes /metrics on the existing HTTP listener when
            true.
          type: boolean
      type: object
    config.OAuthConfig:
      description: OAuth configuration (keep even when empty to signal OAuth requirement)
      properties:
        client_id:
          type: string
        client_secret:
          type: string
        extra_params:
          additionalProperties:
            type: string
          description: Additional OAuth parameters (e.g., RFC 8707 resource)
          type: object
        pkce_enabled:
          type: boolean
        redirect_uri:
          type: string
        scopes:
          items:
            type: string
          type: array
          uniqueItems: false
      type: object
    config.ObservabilityConfig:
      description: 'Observability settings (Spec 069): usage aggregate cache/persistence
        cadence.'
      properties:
        metrics:
          $ref: '#/components/schemas/config.MetricsExporterConfig'
        tracing:
          $ref: '#/components/schemas/config.TracingExporterConfig'
        usage_cache_ttl:
          description: |-
            UsageCacheTTL bounds the freshness of the usage endpoint's read cache for
            wide windows (FR-005). Default 5s.
          type: string
        usage_persist_interval:
          description: |-
            UsagePersistInterval is how often the actor-owned usage aggregate snapshot
            is flushed to storage. Default 30s.
          type: string
      type: object
    config.OutputSanitisationConfig:
      description: Output sanitisation settings (Spec 054 Track B)
      properties:
        max_redactions:
          description: cap on redactions per response; default 100
          type: integer
        response_action:
          description: '"spotlight" | "redact" | "block"; default "spotlight"'
          type: string
        spotlight_untrusted:
          description: wrap untrusted output in spotlight markers; default true
          type: boolean
        strip_classes:
          description: 'classes to strip: ansi/c0c1/bidi/zero_width'
          items:
            type: string
          type: array
          uniqueItems: false
        strip_control_chars:
          description: strip control-character classes; default false
          type: boolean
      type: object
    config.OutputValidationConfig:
      description: Output-schema validation settings (Spec 056)
      properties:
        max_bytes:
          description: structured payload byte cap; default 5<<20
          type: integer
        max_depth:
          description: nesting depth cap; default 64
          type: integer
        missing_structured_content:
          description: '"allow" | "block"; default "allow"'
          type: string
        mode:
          description: '"off" | "warn" | "strict"; default "warn"'
          type: string
      type: object
    config.ProfileConfig:
      properties:
        name:
          description: URL slug, validated
          type: string
        servers:
          description: references to mcpServers[].name
          items:
            type: string
          type: array
          uniqueItems: false
      type: object
    config.RegistryEntry:
      properties:
        count:
          description: number or string
          type: string
        description:
          type: string
        id:
          type: string
        name:
          type: string
        protocol:
          type: string
        provenance:
          description: |-
            Provenance is the trust tag for this registry (MCP-866):
            RegistryProvenanceOfficial for built-in defaults, RegistryProvenanceCustom
            for user-added registries. It is authoritatively (re)computed by the
            registries merge from whether the ID is a shipped default — a user cannot
            claim "official" by writing it into their config.
          type: string
        requires_key:
          description: |-
            RequiresKey marks a registry that needs an API key to be queried. When
            true and no key is configured, the registry is skipped/marked unavailable
            rather than failing the whole search (FR-008).
          type: boolean
        servers_url:
          type: string
        tags:
          items:
            type: string
          type: array
          uniqueItems: false
        url:
          type: string
      type: object
    config.SecurityConfig:
      description: Security scanner settings (Spec 039)
      properties:
        deep_scan:
          $ref: '#/components/schemas/config.DeepScanConfig'
        integrity_check_interval:
          type: string
        integrity_check_on_restart:
          type: boolean
        runtime_read_only:
          type: boolean
        runtime_tmpfs_size:
          type: string
        scan_timeout_default:
          type: string
        scanner_disable_no_new_privileges:
          description: |-
            Deprecated (Spec 077 US3): migrated on load into DeepScan.DisableNoNewPrivileges
            (see migrateDeepScanConfig). Retained only so existing configs that still carry
            the top-level key parse; consumers MUST read the effective value via
            SecurityConfig.IsDisableNoNewPrivileges. Cleared after migration.

            ScannerDisableNoNewPrivileges, when true, omits the
            `--security-opt no-new-privileges` flag from scanner container runs.

            Background: snap-installed Docker on Ubuntu confines dockerd under the
            `snap.docker.dockerd` AppArmor profile. When runc tries to transition
            the container into the inner `docker-default` profile to exec the
            entrypoint, AppArmor refuses the transition because NO_NEW_PRIVS
            forbids privilege/profile changes on exec — the result is EPERM
            ("operation not permitted") and every scanner fails immediately.

            Set this to true ONLY on hosts hitting that incompatibility. Scanner
            containers still run with read-only rootfs, tmpfs /tmp, no-network by
            default, and read-only source mounts, so the marginal isolation loss
            is small. The preferred fix remains replacing snap docker with a
            distro-packaged docker.
          type: boolean
        scanner_fetch_package_source:
          description: |-
            Deprecated (Spec 077 US3): migrated on load into DeepScan.FetchPackageSource
            (see migrateDeepScanConfig). Retained only so existing configs that still carry
            the top-level key parse; consumers MUST read the effective value via
            SecurityConfig.EffectiveFetchPackageSource. Cleared after migration.

            ScannerFetchPackageSource controls whether the scanner fetches the
            PUBLISHED source of package-runner servers (npx/uvx) — without executing
            it — when no local source is available (no Docker container, no local
            package cache, no working_dir). This is the primary quarantine/scan
            target: a quarantined-on-add server is never run locally, so without this
            the scan degrades to tool-definitions-only (no real source-level
            analysis). See MCP-2206.

            Fetching uses `npm pack --ignore-scripts` (npm) and `uv pip download` /
            `pip download` with `--only-binary=:all:` (Python), which only download +
            unpack archives and NEVER run install, build, or setup.py — a scanner must
            not execute the untrusted code it is scanning. The Python
            `--only-binary=:all:` flag is required because downloading an sdist would
            invoke its build backend (setup.py); packages with no wheel fall back to
            tool-definitions-only instead. Extraction is hardened against path
            traversal and decompression bombs.

            Default (nil) is ENABLED. Set to false on air-gapped deployments to
            forbid the scanner's network egress; such servers then fall back to the
            tool-definitions-only scan with no regression.
          type: boolean
        scanner_registry_url:
          type: string
        tpa_bundle_path:
          description: |-
            TPABundlePath is the filesystem path to the tpa-db scanner-bundle.json
            the offline TPA scanner runs (spec 086 FR-019: the signature-DB location
            MUST be configuration-driven, not hardcoded). Empty (the default) runs the
            corpus embedded in this build.

            Env override: MCPPROXY_TPA_BUNDLE_PATH. Hot-reloadable — the path is
            re-read on every config.reloaded event via
            scanner.Service.ApplySecurityConfig, so a corpus refresh needs no restart.
            A configured bundle that fails to read/parse/version-check/compile is
            REFUSED and the previously active corpus stays live (fail-closed, never
            fail-empty); the reason is logged and surfaced in the security overview's
            signature_bundle.load_error.
          type: string
      type: object
    config.SensitiveDataDetectionConfig:
      description: Sensitive data detection settings (Spec 026)
      properties:
        categories:
          additionalProperties:
            type: boolean
          description: Enable/disable specific detection categories
          type: object
        custom_patterns:
          description: User-defined detection patterns
          items:
            $ref: '#/components/schemas/config.CustomPattern'
          type: array
          uniqueItems: false
        enabled:
          description: 'Enable sensitive data detection (default: true)'
          type: boolean
        entropy_threshold:
          description: 'Shannon entropy threshold for high-entropy detection (default:
            4.5)'
          type: number
        max_payload_size_kb:
          description: 'Max size to scan before truncating (default: 1024)'
          type: integer
        scan_requests:
          description: 'Scan tool call arguments (default: true)'
          type: boolean
        scan_responses:
          description: 'Scan tool responses (default: true)'
          type: boolean
        sensitive_keywords:
          description: Keywords to flag
          items:
            type: string
          type: array
          uniqueItems: false
      type: object
    config.ServerConfig:
      properties:
        args:
          items:
            type: string
          type: array
          uniqueItems: false
        auto_approve_tool_changes:
          description: |-
            AutoApproveToolChanges is the per-server intent to auto-approve tool
            changes/additions (disabling per-server rug-pull protection). Supersedes
            skip_quarantine. MCP-2930 only ACCEPTS, persists, and migrates this flag — it
            is NOT yet consulted at runtime; auto-approval is still governed by
            SkipQuarantine until the trust-baseline behavior change (MCP-2931) migrates the
            runtime consumers onto it.
            Tri-state pointer (mirrors QuarantineEnabled): nil = unset (inherit/migrate
            from legacy skip_quarantine), explicit true/false = honored as-is so an
            explicit auto_approve_tool_changes:false overrides a legacy skip_quarantine:true.
            Read via IsAutoApproveToolChanges().
          type: boolean
        command:
          type: string
        created:
          type: string
        disabled_tools:
          description: 'Denylist: these tools are hidden; mutually exclusive with
            enabled_tools'
          items:
            type: string
          type: array
          uniqueItems: false
        enabled:
          type: boolean
        enabled_tools:
          description: 'Allowlist: only these tools are exposed; mutually exclusive
            with disabled_tools'
          items:
            type: string
          type: array
          uniqueItems: false
        env:
          additionalProperties:
            type: string
          type: object
        expose_prompts:
          description: |-
            ExposePrompts overrides whether this server's advertised MCP prompts are
            aggregated into mcpproxy's prompts/list. nil (default) inherits the
            default-aggregate behavior (included if the server advertises
            Capabilities.Prompts); false excludes it regardless of capability.
          type: boolean
        headers:
          additionalProperties:
            type: string
          description: For HTTP servers
          type: object
        health_check_interval:
          description: |-
            Per-server discovery & health-check overrides (spec 074). Same *Duration
            tri-state as the global keys: nil = inherit the global value (or default),
            pointer to 0s = disabled for this server, positive = that interval.
            HealthCheckInterval is fully wired into the per-server health loop;
            ToolDiscoveryInterval is accepted/validated and round-trips for
            forward-compat, but the periodic index sweep is governed by the global
            cadence in this iteration (see spec 074 plan §C).
          type: string
        init_timeout:
          description: |-
            InitTimeout overrides the global init_timeout for this server's MCP
            `initialize` handshake deadline (MCP-3322 / GH #760). *Duration tri-state:
            nil = inherit the global value (or 30s default), positive = that deadline.
            Resolved by Config.ResolveInitTimeout; validated to {0} ∪ [1s, 30m]. Raise
            this for upstreams that do legitimate first-run warmup (e.g. caching many
            channels/users) before responding to `initialize`.
          type: string
        isolation:
          $ref: '#/components/schemas/config.IsolationConfig'
        launcher_wait_timeout:
          description: |-
            LauncherWaitTimeout caps how long mcpproxy will wait for a locally-launched
            HTTP/SSE upstream's URL to become reachable after Spawn(). Only consulted
            when the server is configured with both Command and an HTTP/SSE URL — i.e.,
            mcpproxy starts the process AND connects via network. Stdio servers ignore
            this field. Zero or unset → 30s default.
          type: string
        max_concurrent_requests:
          description: |-
            Per-server concurrency overrides — scope (c) of FR-020 (spec 093, #955).
            Tri-state per setting, exactly like HealthCheckInterval: absent = inherit
            the per-server default set (server_concurrency_defaults), explicit 0 =
            disable that setting for this server (0 max = no per-server limiter at
            all; 0 queue_size = no pending capacity, shed immediately at the cap),
            positive = override. The global aggregate limiter is never inherited from
            here — it applies on top, so effective concurrency is min(per-server,
            global). Resolved by Config.ResolveServerConcurrency.
          type: integer
        name:
          type: string
        oauth:
          $ref: '#/components/schemas/config.OAuthConfig'
        protocol:
          description: stdio, http, sse, streamable-http, auto
          type: string
        quarantined:
          description: Security quarantine status
          type: boolean
        queue_size:
          type: integer
        queue_timeout:
          type: string
        reconnect_on_use:
          description: Attempt reconnection when a tool call targets a disconnected
            server
          type: boolean
        shared:
          description: 'Server edition: shared with all users'
          type: boolean
        skip_quarantine:
          description: |-
            SkipQuarantine is DEPRECATED (MCP-2930): use AutoApproveToolChanges instead.
            Kept for back-compat parsing; on config load a legacy skip_quarantine:true is
            migrated to auto_approve_tool_changes:true only when the new field is unset
            (see normalizeServerQuarantineFlags).
          type: boolean
        source_registry_id:
          description: |-
            SourceRegistryID records which registry this server was added from (empty
            for manually-configured servers). MCP-866: surfaced in the approval /
            quarantine view so a reviewer can see a server's origin.
          type: string
        source_registry_provenance:
          description: |-
            SourceRegistryProvenance records the source registry's provenance at add
            time (RegistryProvenanceOfficial / RegistryProvenanceCustom). It is purely
            informational (MCP-1072) — surfaced so a reviewer can see a server's origin
            — and no longer gates quarantine or skip_quarantine.
          type: string
        tool_discovery_interval:
          type: string
        toon_output:
          description: |-
            ToonOutput overrides the global toon_output mode for this server's
            tools (spec 084, FR-001). Plain string, not a pointer: ""/absent =
            inherit the global value; "off"|"adaptive"|"always" = override ("off"
            is the explicit force-off). Resolved by Config.ResolveToonOutput.
          type: string
        trust_mode:
          description: |-
            TrustMode is the per-server trust tier: auto|scan|manual. Supersedes
            auto_approve_tool_changes (spec 086). An empty value is derived from the
            legacy fields at load via normalizeServerQuarantineFlags; the single
            resolution point is EffectiveTrustMode(), which treats an empty or
            unrecognized value as manual (secure by default). Read via
            EffectiveTrustMode(), never the raw string.
          type: string
        updated:
          type: string
        url:
          type: string
        working_dir:
          description: Working directory for stdio servers
          type: string
      type: object
    config.TLSConfig:
      description: TLS configuration
      properties:
        certs_dir:
          description: Directory for certificates
          type: string
        enabled:
          description: Enable HTTPS
          type: boolean
        hsts:
          description: Enable HTTP Strict Transport Security
          type: boolean
        require_client_cert:
          description: Enable mTLS
          type: boolean
      type: object
    config.TelemetryConfig:
      description: Telemetry settings (Spec 036)
      properties:
        anonymous_id:
          description: Auto-generated UUIDv4
          type: string
        anonymous_id_created_at:
          description: Spec 042 (Tier 2) additions — all default-zero, all backwards-compatible.
          type: string
        enabled:
          description: 'Default: true (opt-out)'
          type: boolean
        endpoint:
          description: Override for testing
          type: string
        last_reported_version:
          description: Upgrade funnel
          type: string
        last_startup_outcome:
          description: success|port_conflict|db_locked|...
          type: string
        notice_shown:
          description: First-run notice flag
          type: boolean
      type: object
    config.TokenizerConfig:
      description: Tokenizer configuration for token counting
      properties:
        default_model:
          description: Default model for tokenization (e.g., "gpt-4")
          type: string
        enabled:
          description: Enable token counting
          type: boolean
        encoding:
          description: Default encoding (e.g., "cl100k_base")
          type: string
      type: object
    config.TracingExporterConfig:
      description: |-
        Tracing gates the OpenTelemetry OTLP trace exporter (MCP-32). Disabled by
        default.
      properties:
        enabled:
          description: Enabled turns on OTLP trace export for tool calls and upstream
            hops.
          type: boolean
        endpoint:
          description: |-
            Endpoint is the collector address as host:port (no scheme), e.g.
            "localhost:4318" for http or "localhost:4317" for grpc.
          type: string
        protocol:
          description: 'Protocol selects the OTLP transport: "http" or "grpc".'
          type: string
        sample_rate:
          description: SampleRate is the head-based trace sampling ratio in [0,1].
            Default 0.1.
          type: number
      type: object
    config.UpdateCheckConfig:
      description: |-
        Update-check settings (Spec 079 FR-012): config-file control of the
        background upgrade-awareness checker (internal/updatecheck). nil =
        enabled on the stable channel (existing default behavior). The existing
        environment switches keep working and WIN over these keys (FR-014):
        MCPPROXY_DISABLE_AUTO_UPDATE=true force-disables even when
        enabled=true, and MCPPROXY_ALLOW_PRERELEASE_UPDATES=true force-selects
        the rc channel even when channel=stable.
      properties:
        channel:
          description: |-
            Channel selects which releases are offered as updates: "stable"
            (default; prereleases never offered) or "rc" (prereleases included).
            Empty resolves to stable. Validated in ValidateDetailed.

            NOTE: for a RELEASED build the running binary's own version is
            authoritative and overrides this field — a stable build is never
            offered an RC (even with channel=rc), and an RC build always tracks the
            rc channel. This field only takes effect on dev/unstamped builds. See
            internal/updatecheck.Checker.IncludePrereleases.
          type: string
        enabled:
          description: |-
            Enabled gates all update checking. Tri-state: nil/absent = enabled
            (default true, matching pre-079 behavior). When false, no network
            check is performed and no upgrade nudge appears on any surface
            (FR-015) — /api/v1/info omits the update object entirely.
          type: boolean
      type: object
    configimport.FailedServer:
      properties:
        details:
          type: string
        error:
          type: string
        name:
          type: string
      type: object
    configimport.ImportSummary:
      properties:
        failed:
          type: integer
        imported:
          type: integer
        skipped:
          type: integer
        total:
          type: integer
      type: object
    configimport.SkippedServer:
      properties:
        name:
          type: string
        reason:
          description: '"already_exists", "filtered_out", "invalid_name"'
          type: string
      type: object
    connect.ConnectResult:
      description: The full result; its action mirrors the top-level one
      properties:
        action:
          description: '"created", "updated", "already_exists", "removed", "not_found"'
          type: string
        backup_path:
          type: string
        client:
          type: string
        config_path:
          type: string
        message:
          type: string
        server_name:
          type: string
        success:
          type: boolean
      type: object
    contracts.APIResponse:
      allOf:
      - $ref: '#/components/schemas/data'
      properties:
        data:
          type: object
        error:
          type: string
        request_id:
          type: string
        success:
          type: boolean
      type: object
    contracts.ActivityDetailResponse:
      properties:
        activity:
          $ref: '#/components/schemas/contracts.ActivityRecord'
      type: object
    contracts.ActivityListResponse:
      properties:
        activities:
          items:
            $ref: '#/components/schemas/contracts.ActivityRecord'
          type: array
          uniqueItems: false
        limit:
          type: integer
        offset:
          type: integer
        total:
          type: integer
      type: object
    contracts.ActivityRecord:
      properties:
        arguments:
          description: Tool call arguments
          type: object
        detection_types:
          description: List of detection types found
          items:
            type: string
          type: array
          uniqueItems: false
        duration_ms:
          description: Execution duration in milliseconds
          type: integer
        error_message:
          description: Error details if status is "error"
          type: string
        has_sensitive_data:
          description: Sensitive data detection fields (Spec 026)
          type: boolean
        id:
          description: Unique identifier (ULID format)
          type: string
        max_severity:
          description: Highest severity level detected (critical, high, medium, low)
          type: string
        metadata:
          description: Additional context-specific data
          type: object
        request_id:
          description: HTTP request ID for correlation
          type: string
        response:
          description: Tool response (potentially truncated)
          type: string
        response_truncated:
          description: True if response was truncated
          type: boolean
        server_name:
          description: Name of upstream MCP server
          type: string
        session_id:
          description: MCP transport session ID (regenerated on every reconnect)
          type: string
        source:
          $ref: '#/components/schemas/contracts.ActivitySource'
        status:
          description: 'Result status: "success", "error", "blocked", "rejected"'
          type: string
        timestamp:
          description: When activity occurred
          type: string
        tool_name:
          description: Name of tool called
          type: string
        type:
          $ref: '#/components/schemas/contracts.ActivityType'
        work_session_id:
          description: 'Spec 082: one client, one project, across reconnects'
          type: string
      type: object
    contracts.ActivitySource:
      description: 'How activity was triggered: "mcp", "cli", "api"'
      type: string
      x-enum-varnames:
      - ActivitySourceMCP
      - ActivitySourceCLI
      - ActivitySourceAPI
    contracts.ActivitySummaryResponse:
      properties:
        blocked_count:
          description: Count of blocked activities
          type: integer
        end_time:
          description: End of the period (RFC3339)
          type: string
        error_count:
          description: Count of error activities
          type: integer
        period:
          description: Time period (1h, 24h, 7d, 30d)
          type: string
        rejected_count:
          description: |-
            RejectedCount is the number of calls shed by a concurrency limiter before
            they reached an upstream (spec 093). Counted separately from errors: it is
            proxy backpressure, not an upstream fault, and it is the signal an
            operator right-sizes max_concurrent_requests against.
          type: integer
        start_time:
          description: Start of the period (RFC3339)
          type: string
        success_count:
          description: Count of successful activities
          type: integer
        top_servers:
          description: Top servers by activity count
          items:
            $ref: '#/components/schemas/contracts.ActivityTopServer'
          type: array
          uniqueItems: false
        top_tools:
          description: Top tools by activity count
          items:
            $ref: '#/components/schemas/contracts.ActivityTopTool'
          type: array
          uniqueItems: false
        total_count:
          description: Total activity count
          type: integer
      type: object
    contracts.ActivityTopServer:
      properties:
        count:
          description: Activity count
          type: integer
        name:
          description: Server name
          type: string
      type: object
    contracts.ActivityTopTool:
      properties:
        count:
          description: Activity count
          type: integer
        server:
          description: Server name
          type: string
        tool:
          description: Tool name
          type: string
      type: object
    contracts.ActivityType:
      description: Type of activity
      type: string
      x-enum-varnames:
      - ActivityTypeToolCall
      - ActivityTypePolicyDecision
      - ActivityTypeQuarantineChange
      - ActivityTypeServerChange
    contracts.AddFromRegistryRequest:
      properties:
        enabled:
          description: defaults to true when nil
          type: boolean
        env:
          additionalProperties:
            type: string
          description: overrides + required-input values
          type: object
        name:
          description: optional name override
          type: string
      type: object
    contracts.AddRegistrySourceRequest:
      properties:
        id:
          description: derived from the host when empty
          type: string
        name:
          description: defaults to the id
          type: string
        protocol:
          description: defaults to modelcontextprotocol/registry
          type: string
        url:
          description: required https registry URL
          type: string
      type: object
    contracts.ConfigApplyResult:
      properties:
        applied_immediately:
          type: boolean
        changed_fields:
          items:
            type: string
          type: array
          uniqueItems: false
        requires_restart:
          type: boolean
        restart_reason:
          type: string
        success:
          type: boolean
        validation_errors:
          items:
            $ref: '#/components/schemas/contracts.ValidationError'
          type: array
          uniqueItems: false
      type: object
    contracts.DCRStatus:
      properties:
        attempted:
          type: boolean
        error:
          type: string
        status_code:
          type: integer
        success:
          type: boolean
      type: object
    contracts.DeepScanDescriptor:
      description: |-
        DeepScan reports the opt-in "deep scan" layer status (Spec 077 US3),
        SEPARATELY from the baseline verdict above. Always emitted on a computed
        summary — when deep scan is off (the default) it reports enabled=false
        plus any enabled-but-skipped Docker scanners. It never influences Status.
      properties:
        available:
          type: boolean
        enabled:
          type: boolean
        ran:
          type: boolean
        scanners_failed:
          items:
            $ref: '#/components/schemas/contracts.DeepScanScannerFailure'
          type: array
          uniqueItems: false
        skipped_scanners:
          description: |-
            SkippedScanners lists Docker scanners the user enabled that are skipped
            because security.deep_scan.enabled is false (informational).
          items:
            type: string
          type: array
          uniqueItems: false
      type: object
    contracts.DeepScanScannerFailure:
      properties:
        id:
          type: string
        reason:
          type: string
      type: object
    contracts.DeprecatedConfigWarning:
      properties:
        field:
          type: string
        message:
          type: string
        replacement:
          type: string
      type: object
    contracts.Diagnostic:
      description: |-
        Spec 044 — structured diagnostic error and stable error code. Both
        are populated when the server is in a failed state and the error
        has been classified by internal/diagnostics. Healthy servers omit
        these fields.
      properties:
        cause:
          type: string
        code:
          type: string
        detected_at:
          type: string
        docs_url:
          type: string
        fix_steps:
          items:
            $ref: '#/components/schemas/contracts.DiagnosticFixStep'
          type: array
          uniqueItems: false
        severity:
          type: string
        user_message:
          type: string
      type: object
    contracts.DiagnosticFixStep:
      properties:
        command:
          type: string
        destructive:
          type: boolean
        fixer_key:
          type: string
        label:
          type: string
        type:
          type: string
        url:
          type: string
      type: object
    contracts.Diagnostics:
      properties:
        deprecated_configs:
          description: Deprecated config fields found
          items:
            $ref: '#/components/schemas/contracts.DeprecatedConfigWarning'
          type: array
          uniqueItems: false
        docker_status:
          $ref: '#/components/schemas/contracts.DockerStatus'
        missing_secrets:
          description: Renamed to avoid conflict
          items:
            $ref: '#/components/schemas/contracts.MissingSecretInfo'
          type: array
          uniqueItems: false
        oauth_issues:
          description: OAuth parameter mismatches
          items:
            $ref: '#/components/schemas/contracts.OAuthIssue'
          type: array
          uniqueItems: false
        oauth_required:
          items:
            $ref: '#/components/schemas/contracts.OAuthRequirement'
          type: array
          uniqueItems: false
        runtime_warnings:
          items:
            type: string
          type: array
          uniqueItems: false
        timestamp:
          type: string
        total_issues:
          type: integer
        upstream_errors:
          items:
            $ref: '#/components/schemas/contracts.UpstreamError'
          type: array
          uniqueItems: false
      type: object
    contracts.DockerStatus:
      properties:
        available:
          type: boolean
        error:
          type: string
        version:
          type: string
      type: object
    contracts.EditRegistrySourceRequest:
      properties:
        name:
          description: new display name
          type: string
        servers_url:
          description: explicit servers-collection URL
          type: string
        url:
          description: new base/servers https URL
          type: string
      type: object
    contracts.ErrorResponse:
      properties:
        error:
          type: string
        request_id:
          type: string
        success:
          type: boolean
      type: object
    contracts.FindingCounts:
      properties:
        dangerous:
          description: Tool poisoning, active prompt injection
          type: integer
        info:
          description: Low-severity CVEs, informational
          type: integer
        total:
          type: integer
        warning:
          description: Rug pull, supply chain CVEs with exploits
          type: integer
      type: object
    contracts.GetConfigResponse:
      properties:
        config:
          description: The configuration object
          type: object
        config_path:
          description: Path to config file
          type: string
      type: object
    contracts.GetRegistriesResponse:
      properties:
        registries:
          items:
            $ref: '#/components/schemas/contracts.Registry'
          type: array
          uniqueItems: false
        total:
          type: integer
      type: object
    contracts.GetServerLogsResponse:
      properties:
        count:
          type: integer
        logs:
          items:
            $ref: '#/components/schemas/contracts.LogEntry'
          type: array
          uniqueItems: false
        server_name:
          type: string
      type: object
    contracts.GetServerToolCallsResponse:
      properties:
        server_name:
          type: string
        tool_calls:
          items:
            $ref: '#/components/schemas/contracts.ToolCallRecord'
          type: array
          uniqueItems: false
        total:
          type: integer
      type: object
    contracts.GetServerToolsResponse:
      properties:
        count:
          type: integer
        server_name:
          type: string
        tools:
          items:
            $ref: '#/components/schemas/contracts.Tool'
          type: array
          uniqueItems: false
      type: object
    contracts.GetServersResponse:
      properties:
        servers:
          items:
            $ref: '#/components/schemas/contracts.Server'
          type: array
          uniqueItems: false
        stats:
          $ref: '#/components/schemas/contracts.ServerStats'
      type: object
    contracts.GetSessionDetailResponse:
      properties:
        session:
          $ref: '#/components/schemas/contracts.MCPSession'
      type: object
    contracts.GetSessionsResponse:
      properties:
        limit:
          type: integer
        offset:
          type: integer
        sessions:
          items:
            $ref: '#/components/schemas/contracts.MCPSession'
          type: array
          uniqueItems: false
        total:
          type: integer
      type: object
    contracts.GetToolCallDetailResponse:
      properties:
        tool_call:
          $ref: '#/components/schemas/contracts.ToolCallRecord'
      type: object
    contracts.GetToolCallsResponse:
      properties:
        limit:
          type: integer
        offset:
          type: integer
        tool_calls:
          items:
            $ref: '#/components/schemas/contracts.ToolCallRecord'
          type: array
          uniqueItems: false
        total:
          type: integer
      type: object
    contracts.GlobalToolsResponse:
      properties:
        failed_servers:
          items:
            type: string
          type: array
          uniqueItems: false
        partial:
          type: boolean
        stats:
          $ref: '#/components/schemas/contracts.GlobalToolsStats'
        tools:
          items:
            $ref: '#/components/schemas/contracts.Tool'
          type: array
          uniqueItems: false
      type: object
    contracts.GlobalToolsStats:
      properties:
        disabled:
          type: integer
        enabled:
          type: integer
        pending_approval:
          type: integer
        total:
          type: integer
      type: object
    contracts.HealthStatus:
      description: Unified health status calculated by the backend
      properties:
        action:
          description: 'Action is the suggested fix action: "login", "restart", "enable",
            "approve", "view_logs", "set_secret", "configure", or "" (none)'
          type: string
        admin_state:
          description: 'AdminState indicates the admin state: "enabled", "disabled",
            or "quarantined"'
          type: string
        detail:
          description: Detail is an optional longer explanation of the status
          type: string
        level:
          description: 'Level indicates the health level: "healthy", "degraded", or
            "unhealthy"'
          type: string
        summary:
          description: Summary is a human-readable status message (e.g., "Connected
            (5 tools)")
          type: string
      type: object
    contracts.InfoEndpoints:
      description: Available API endpoints
      properties:
        http:
          description: HTTP endpoint address (e.g., "127.0.0.1:8080")
          type: string
        socket:
          description: Unix socket path (empty if disabled)
          type: string
      type: object
    contracts.InfoResponse:
      properties:
        endpoints:
          $ref: '#/components/schemas/contracts.InfoEndpoints'
        launched_by:
          description: |-
            LaunchedBy is the durable launch provenance of the running core (Spec
            092 FR-001a): "tray" when a tray spawned it, "installer" when the macOS
            PKG postinstall did, "" when user-launched or unknown. Always present
            (possibly empty) so a tray can distinguish "old core, not mine" from
            "old core I may supersede".
          type: string
        listen_addr:
          description: Listen address (e.g., "127.0.0.1:8080")
          type: string
        pid:
          description: |-
            PID is the operating-system process id of the running core (Spec 092
            FR-002). A tray that merely ATTACHED to a core holds no Process handle
            for it, so without this there is no mechanism at all to stop a stale
            core — the consent action would have nothing to act on and could only
            print instructions. Paired with LaunchedBy it is what lets a newer tray
            supersede a core an older tray started.
          type: integer
        update:
          $ref: '#/components/schemas/contracts.UpdateInfo'
        update_policy:
          $ref: '#/components/schemas/contracts.UpdatePolicy'
        version:
          description: Current MCPProxy version
          type: string
        web_ui_url:
          description: URL to access the web control panel
          type: string
      type: object
    contracts.IsolationConfig:
      properties:
        cpu_limit:
          type: string
        enabled:
          type: boolean
        extra_args:
          items:
            type: string
          type: array
          uniqueItems: false
        image:
          type: string
        memory_limit:
          type: string
        network_mode:
          type: string
        timeout:
          type: string
        working_dir:
          type: string
      type: object
    contracts.IsolationDefaults:
      description: |-
        IsolationDefaults exposes the resolved baseline values that
        would apply when no per-server override is set. Populated on
        list/get responses; never consumed on PATCH requests.
      properties:
        extra_args:
          items:
            type: string
          type: array
          uniqueItems: false
        image:
          type: string
        network_mode:
          type: string
        runtime_type:
          type: string
        working_dir:
          type: string
      type: object
    contracts.LogEntry:
      properties:
        fields:
          type: object
        level:
          type: string
        message:
          type: string
        server:
          type: string
        timestamp:
          type: string
      type: object
    contracts.MCPSession:
      properties:
        client_name:
          type: string
        client_version:
          type: string
        end_time:
          type: string
        experimental:
          items:
            type: string
          type: array
          uniqueItems: false
        has_roots:
          description: MCP Client Capabilities
          type: boolean
        has_sampling:
          type: boolean
        id:
          type: string
        last_activity:
          type: string
        start_time:
          type: string
        status:
          type: string
        tool_call_count:
          type: integer
        total_tokens:
          type: integer
        work_session_id:
          type: string
        workspace_name:
          description: |-
            Workspace / work session (Spec 082). WorkspaceName is the project's
            basename — the full local path is never exposed. WorkSessionID groups the
            reconnects that make up one stretch of user work.
          type: string
      type: object
    contracts.MetadataStatus:
      properties:
        authorization_servers:
          items:
            type: string
          type: array
          uniqueItems: false
        error:
          type: string
        found:
          type: boolean
        url_checked:
          type: string
      type: object
    contracts.MissingSecretInfo:
      properties:
        secret_name:
          type: string
        used_by:
          items:
            type: string
          type: array
          uniqueItems: false
      type: object
    contracts.NPMPackageInfo:
      properties:
        exists:
          type: boolean
        install_cmd:
          type: string
      type: object
    contracts.OAuthConfig:
      properties:
        auth_url:
          type: string
        client_id:
          type: string
        extra_params:
          additionalProperties:
            type: string
          type: object
        pkce_enabled:
          type: boolean
        redirect_port:
          type: integer
        scopes:
          items:
            type: string
          type: array
          uniqueItems: false
        token_expires_at:
          description: When the OAuth token expires
          type: string
        token_url:
          type: string
        token_valid:
          description: Whether token is currently valid
          type: boolean
      type: object
    contracts.OAuthErrorDetails:
      description: Structured discovery/failure details
      properties:
        authorization_server_metadata:
          $ref: '#/components/schemas/contracts.MetadataStatus'
        dcr_status:
          $ref: '#/components/schemas/contracts.DCRStatus'
        protected_resource_metadata:
          $ref: '#/components/schemas/contracts.MetadataStatus'
        server_url:
          type: string
      type: object
    contracts.OAuthFlowError:
      properties:
        correlation_id:
          description: Flow tracking ID for log correlation
          type: string
        debug_hint:
          description: CLI command for log lookup
          type: string
        details:
          $ref: '#/components/schemas/contracts.OAuthErrorDetails'
        error_code:
          description: Machine-readable error code (e.g., OAUTH_NO_METADATA)
          type: string
        error_type:
          description: Category of OAuth runtime failure
          type: string
        message:
          description: Human-readable error description
          type: string
        request_id:
          description: 'HTTP request ID (from PR #237)'
          type: string
        server_name:
          description: Server that failed OAuth
          type: string
        success:
          description: Always false
          type: boolean
        suggestion:
          description: Actionable remediation hint
          type: string
      type: object
    contracts.OAuthIssue:
      properties:
        documentation_url:
          type: string
        error:
          type: string
        issue:
          type: string
        missing_params:
          items:
            type: string
          type: array
          uniqueItems: false
        resolution:
          type: string
        server_name:
          type: string
      type: object
    contracts.OAuthRequirement:
      properties:
        expires_at:
          type: string
        message:
          type: string
        server_name:
          type: string
        state:
          type: string
      type: object
    contracts.OAuthStartResponse:
      properties:
        auth_url:
          description: Authorization URL (always included for manual use)
          type: string
        browser_error:
          description: Error message if browser launch failed
          type: string
        browser_opened:
          description: Whether browser launch succeeded
          type: boolean
        correlation_id:
          description: UUID for tracking this flow
          type: string
        message:
          description: Human-readable status message
          type: string
        server_name:
          description: Name of the server being authenticated
          type: string
        success:
          description: Always true for successful start
          type: boolean
      type: object
    contracts.PreflightPolicy:
      properties:
        exclude_destructive:
          type: boolean
        exclude_open_world:
          type: boolean
        read_only_only:
          type: boolean
      type: object
    contracts.PreflightReason:
      type: string
      x-enum-varnames:
      - PreflightReasonServerInitializing
      - PreflightReasonServerUnhealthy
      - PreflightReasonServerDisabled
      - PreflightReasonServerQuarantined
      - PreflightReasonToolPendingApproval
      - PreflightReasonToolChanged
      - PreflightReasonToolBlockedByUser
      - PreflightReasonOAuthRequired
      - PreflightReasonHashMismatch
      - PreflightReasonServerNotInScope
      - PreflightReasonToolDeniedByConfig
      - PreflightReasonMissingAnnotation
      - PreflightReasonPolicyFiltered
      - PreflightReasonNotFound
      - PreflightReasonServerNotConfigured
    contracts.PreflightRequest:
      properties:
        policy:
          $ref: '#/components/schemas/contracts.PreflightPolicy'
        profile:
          description: 'Profile evaluates under a named profile''s server scope. Unknown:
            400.'
          type: string
        tools:
          description: |-
            Tools is 1..100 entries BEFORE dedup; duplicates are collapsed, and
            duplicate ids carrying different pins are a validation error.
          items:
            $ref: '#/components/schemas/contracts.PreflightToolRef'
          type: array
          uniqueItems: false
        wait_ms:
          description: |-
            WaitMS polls local state for up to this many milliseconds (cap 10000)
            while every failure is retryable-class.
          type: integer
      type: object
    contracts.PreflightResponse:
      properties:
        checked_at:
          type: string
        tools:
          description: Tools are ordered by first occurrence of each unique id in
            the request.
          items:
            $ref: '#/components/schemas/contracts.PreflightToolResult'
          type: array
          uniqueItems: false
        verdict:
          $ref: '#/components/schemas/contracts.PreflightVerdict'
        waited_ms:
          description: |-
            WaitedMS is present when wait_ms was requested (0 when the wait
            semaphore was exhausted and the request resolved immediately).
          type: integer
      type: object
    contracts.PreflightStatus:
      type: string
      x-enum-varnames:
      - PreflightStatusReady
      - PreflightStatusUnavailable
    contracts.PreflightToolRef:
      properties:
        id:
          description: |-
            ID is a canonical "<server>:<tool>" id. A malformed id is answered with a
            per-ID not_found carrying a format hint, never a request-level error.
          type: string
        pin_hash:
          description: |-
            PinHash is "sha256/v{N}:{hex}" — the schema version is embedded so a
            proxy-side hash-algorithm bump is distinguishable from upstream drift.
          type: string
      type: object
    contracts.PreflightToolResult:
      properties:
        action:
          type: string
        detail:
          type: string
        did_you_mean:
          description: |-
            DidYouMean carries up to 3 nearest caller-visible ids on not_found. It
            never crosses a scope boundary and never names a quarantined server's
            tools.
          items:
            type: string
          type: array
          uniqueItems: false
        hash:
          description: |-
            Hash is the tool's current pin ("sha256/v{N}:{hex}") — operator tier,
            ready results only. Never disclosed to an agent token.
          type: string
        id:
          type: string
        reason:
          $ref: '#/components/schemas/contracts.PreflightReason'
        remediation:
          type: string
        retryable:
          type: boolean
        status:
          $ref: '#/components/schemas/contracts.PreflightStatus'
      type: object
    contracts.PreflightVerdict:
      type: string
      x-enum-varnames:
      - PreflightVerdictReady
      - PreflightVerdictDegradedRetryable
      - PreflightVerdictBlocked
      - PreflightVerdictUnknownIDs
    contracts.QuarantineStats:
      description: Tool quarantine metrics for this server
      properties:
        blocked_count:
          description: Number of disabled (blocked) tools
          type: integer
        changed_count:
          description: Number of tools whose description/schema changed since approval
          type: integer
        pending_count:
          description: Number of newly discovered tools awaiting approval
          type: integer
      type: object
    contracts.RefreshRegistryResponse:
      properties:
        cleared:
          description: number of cached entries dropped
          type: integer
        registry_id:
          type: string
      type: object
    contracts.Registry:
      properties:
        count:
          description: number or string
          type: string
        description:
          type: string
        id:
          type: string
        name:
          type: string
        protocol:
          type: string
        provenance:
          description: |-
            Provenance is the trust tag (MCP-866): "official/trusted" for built-in
            defaults, "custom/unverified" for user-added registries.
          type: string
        servers_url:
          type: string
        tags:
          items:
            type: string
          type: array
          uniqueItems: false
        trusted:
          description: |-
            Trusted indicates whether this is an official, shipped-by-default
            registry. Trust is derived from membership in the default set, never
            from self-assertion in config.
          type: boolean
        url:
          type: string
      type: object
    contracts.RegistryCacheInfo:
      properties:
        age_seconds:
          type: number
        stale:
          type: boolean
      type: object
    contracts.RegistryUnavailable:
      properties:
        reason:
          type: string
      type: object
    contracts.ReplayToolCallRequest:
      properties:
        arguments:
          description: Modified arguments for replay
          type: object
      type: object
    contracts.ReplayToolCallResponse:
      properties:
        error:
          description: Error if replay failed
          type: string
        new_call_id:
          description: ID of the newly created call
          type: string
        new_tool_call:
          $ref: '#/components/schemas/contracts.ToolCallRecord'
        replayed_from:
          description: Original call ID
          type: string
        success:
          type: boolean
      type: object
    contracts.RepositoryInfo:
      description: Detected package info
      properties:
        npm:
          $ref: '#/components/schemas/contracts.NPMPackageInfo'
      type: object
    contracts.RepositoryServer:
      properties:
        connect_url:
          description: Alternative connection URL
          type: string
        created_at:
          type: string
        description:
          type: string
        id:
          type: string
        install_cmd:
          description: Installation command
          type: string
        name:
          type: string
        registry:
          description: Which registry this came from
          type: string
        repository_info:
          $ref: '#/components/schemas/contracts.RepositoryInfo'
        source_code_url:
          description: Source repository URL
          type: string
        updated_at:
          type: string
        url:
          description: MCP endpoint for remote servers only
          type: string
      type: object
    contracts.SearchRegistryServersResponse:
      properties:
        cache:
          $ref: '#/components/schemas/contracts.RegistryCacheInfo'
        query:
          type: string
        registry_id:
          type: string
        servers:
          items:
            $ref: '#/components/schemas/contracts.RepositoryServer'
          type: array
          uniqueItems: false
        tag:
          type: string
        total:
          type: integer
        unavailable:
          $ref: '#/components/schemas/contracts.RegistryUnavailable'
      type: object
    contracts.SearchResult:
      properties:
        matches:
          type: integer
        score:
          type: number
        snippet:
          type: string
        tool:
          $ref: '#/components/schemas/contracts.Tool'
      type: object
    contracts.SearchToolsResponse:
      properties:
        query:
          type: string
        results:
          items:
            $ref: '#/components/schemas/contracts.SearchResult'
          type: array
          uniqueItems: false
        took:
          type: string
        total:
          type: integer
      type: object
    contracts.SecurityScanSummary:
      description: Latest security scan results summary
      properties:
        deep_scan:
          $ref: '#/components/schemas/contracts.DeepScanDescriptor'
        finding_counts:
          $ref: '#/components/schemas/contracts.FindingCounts'
        last_scan_at:
          type: string
        risk_score:
          description: 0-100
          type: integer
        scanners_failed:
          type: integer
        scanners_run:
          description: |-
            Scanner coverage for the primary (baseline) scan pass — informational only.
            Spec 077 US3 (FR-008/FR-014): Status is derived SOLELY from the
            deterministic baseline findings; a failed Docker deep scanner no longer
            downgrades a clean verdict. That failure is surfaced via DeepScan instead.
          type: integer
        scanners_total:
          type: integer
        status:
          description: '"clean", "warnings", "dangerous", "failed", "not_scanned",
            "scanning"'
          type: string
      type: object
    contracts.Server:
      properties:
        args:
          items:
            type: string
          type: array
          uniqueItems: false
        authenticated:
          description: OAuth authentication status
          type: boolean
        auto_approve_tool_changes:
          description: |-
            AutoApproveToolChanges mirrors config.ServerConfig.AutoApproveToolChanges
            (MCP-2930): the per-server intent to auto-approve new/changed tools past
            the trust baseline. Tri-state *bool — nil means "never set" (omitted from
            the payload), so the Web UI toggle (MCP-2932) can distinguish unset from
            an explicit false. Read-only on the GET path; PATCH/POST accept it via
            AddServerRequest.
          type: boolean
        command:
          type: string
        connected:
          type: boolean
        connected_at:
          type: string
        connecting:
          type: boolean
        created:
          type: string
        diagnostic:
          $ref: '#/components/schemas/contracts.Diagnostic'
        enabled:
          type: boolean
        env:
          additionalProperties:
            type: string
          type: object
        error_code:
          type: string
        expose_prompts:
          description: |-
            ExposePrompts mirrors config.ServerConfig.ExposePrompts (F9): the per-server
            prompt-aggregation override. Tri-state *bool — nil/omitted means "inherit
            default aggregation". Surfaced on GET so a caller that PATCHed the override
            can read it back; PATCH/POST accept it via AddServerRequest.
          type: boolean
        headers:
          additionalProperties:
            type: string
          type: object
        health:
          $ref: '#/components/schemas/contracts.HealthStatus'
        id:
          type: string
        init_timeout:
          description: |-
            InitTimeout mirrors config.ServerConfig.InitTimeout (MCP-3322 / GH #760):
            the per-server MCP `initialize` handshake deadline override. Serialized as
            a duration string (e.g. "120s"); nil/omitted means "inherit the global
            default". Surfaced on the GET path so clients can read back a configured
            override; PATCH/POST accept it via AddServerRequest.
          type: string
        isolation:
          $ref: '#/components/schemas/contracts.IsolationConfig'
        isolation_defaults:
          $ref: '#/components/schemas/contracts.IsolationDefaults'
        last_error:
          type: string
        last_reconnect_at:
          type: string
        last_retry_time:
          type: string
        max_concurrent_requests:
          description: |-
            Spec 093 (GH #955) — per-server concurrency overrides, scope (c) of
            FR-020. Each setting is tri-state: nil (omitted) means "inherit
            server_concurrency_defaults", 0 disables that setting for this server,
            positive overrides it. Surfaced on the GET path so a caller can read back
            what it set; PATCH/POST accept them via AddServerRequest. The effective
            concurrency for a server is additionally bounded by the global aggregate
            limiter, which is NOT an inheritance source for these fields.
          type: integer
        name:
          type: string
        oauth:
          $ref: '#/components/schemas/contracts.OAuthConfig'
        oauth_status:
          description: 'OAuth status: "authenticated", "expired", "error", "none"'
          type: string
        protocol:
          type: string
        quarantine:
          $ref: '#/components/schemas/contracts.QuarantineStats'
        quarantined:
          type: boolean
        queue_size:
          type: integer
        queue_timeout:
          type: string
        reconnect_count:
          type: integer
        reconnect_on_use:
          description: Attempt reconnection when a tool call targets this disconnected
            server
          type: boolean
        retry_count:
          type: integer
        security_scan:
          $ref: '#/components/schemas/contracts.SecurityScanSummary'
        should_retry:
          type: boolean
        source_registry_id:
          description: |-
            MCP-901 — registry provenance of an upstream that was added from a
            registry. SourceRegistryID names the source registry (empty for
            manually-configured servers); SourceRegistryProvenance is the trust tag
            recorded at add time ("official/trusted" or "custom/unverified"). Both
            are projected from config.ServerConfig so the approval/quarantine view
            can render an "added from <registry> · unverified" origin badge. Optional
            and omitted when empty — clients that pre-date this treat them as absent.
          type: string
        source_registry_provenance:
          type: string
        status:
          type: string
        token_expires_at:
          description: When the OAuth token expires (ISO 8601)
          type: string
        tool_count:
          type: integer
        tool_list_token_size:
          description: Token size for this server's tools
          type: integer
        trust_mode:
          description: |-
            TrustMode mirrors config.ServerConfig.TrustMode (spec 086): the per-server
            trust tier ("auto"/"scan"/"manual"). Surfaced on the GET path so clients can
            read back the persisted mode; PATCH/POST accept it via AddServerRequest.
            Omitted when empty (server predates the field / relies on legacy flags).
          type: string
        updated:
          type: string
        url:
          type: string
        user_logged_out:
          description: True if user explicitly logged out (prevents auto-reconnection)
          type: boolean
        working_dir:
          type: string
      type: object
    contracts.ServerActionResponse:
      properties:
        action:
          type: string
        async:
          type: boolean
        server:
          type: string
        success:
          type: boolean
      type: object
    contracts.ServerStats:
      properties:
        connected_servers:
          type: integer
        docker_containers:
          type: integer
        quarantined_servers:
          type: integer
        token_metrics:
          $ref: '#/components/schemas/contracts.ServerTokenMetrics'
        total_servers:
          type: integer
        total_tools:
          type: integer
      type: object
    contracts.ServerTokenMetrics:
      properties:
        average_query_result_size:
          description: Typical retrieve_tools output (tokens)
          type: integer
        per_server_tool_list_sizes:
          additionalProperties:
            type: integer
          description: Token size per server
          type: object
        saved_tokens:
          description: Difference
          type: integer
        saved_tokens_percentage:
          description: Percentage saved
          type: number
        total_server_tool_list_size:
          description: All upstream tools combined (tokens)
          type: integer
      type: object
    contracts.SuccessResponse:
      properties:
        data:
          type: object
        success:
          type: boolean
      type: object
    contracts.TokenMetrics:
      description: Token usage metrics (nil for older records)
      properties:
        encoding:
          description: Encoding used (e.g., cl100k_base)
          type: string
        estimated_cost:
          description: Optional cost estimate
          type: number
        input_tokens:
          description: Tokens in the request
          type: integer
        model:
          description: Model used for tokenization
          type: string
        output_tokens:
          description: Tokens in the response
          type: integer
        total_tokens:
          description: Total tokens (input + output)
          type: integer
        truncated_tokens:
          description: Tokens removed by truncation
          type: integer
        was_truncated:
          description: Whether response was truncated
          type: boolean
      type: object
    contracts.Tool:
      properties:
        annotations:
          $ref: '#/components/schemas/contracts.ToolAnnotation'
        approval_status:
          type: string
        config_denied:
          description: |-
            ConfigDenied is true when the tool is denied by the server's static
            enabled_tools / disabled_tools config. The user cannot override this toggle.
          type: boolean
        description:
          type: string
        disabled:
          description: |-
            Disabled mirrors ToolApprovalRecord.Disabled so per-tool enable state is
            available without a second round-trip to the approvals endpoint. Absent
            in the JSON when false (default) to keep responses compact.
          type: boolean
        hash:
          description: |-
            Hash is the tool's current stored hash rendered in the preflight pin
            format "sha256/v{N}:{hex}" (Spec 098 FR-011), where N is the approval
            record's HashSchemaVersion. It is the authoring surface for
            `POST /api/v1/preflight` pins and `mcpproxy tools preflight --pin`:
            copy the value straight into a pin.

            Disclosure is OPERATOR TIER ONLY — same rule as the preflight per-tool
            result. The field is omitted for agent-token callers and for tools with
            no stored hash (no approval record yet, or a record written before
            hashes existed).
          type: string
        held_reason:
          description: |-
            HeldReason, HeldVerdict and HeldSignals mirror the same-named fields on
            storage.ToolApprovalRecord: the offline-scan evidence that made
            trust_mode: scan hold this tool for review (spec 086 FR-018). HeldSignals
            names the matched deterministic check ids, e.g.
            "tpa.TPA-2026-0001.hidden_instruction", so a reviewer can see WHY the tool
            is held. All three are omitted for tools that are not held by the scan gate
            (including every record written before the field existed).
          type: string
        held_signals:
          items:
            type: string
          type: array
          uniqueItems: false
        held_verdict:
          type: string
        last_used:
          type: string
        name:
          type: string
        schema:
          type: object
        server_name:
          type: string
        usage:
          type: integer
      type: object
    contracts.ToolAnnotation:
      description: Tool behavior hints snapshot
      properties:
        destructiveHint:
          type: boolean
        idempotentHint:
          type: boolean
        openWorldHint:
          type: boolean
        readOnlyHint:
          type: boolean
        title:
          type: string
      type: object
    contracts.ToolCallRecord:
      description: The new tool call record
      properties:
        annotations:
          $ref: '#/components/schemas/contracts.ToolAnnotation'
        arguments:
          description: Tool arguments
          type: object
        config_path:
          description: Active config file path
          type: string
        duration:
          description: Duration in nanoseconds
          type: integer
        error:
          description: Error message (failure only)
          type: string
        execution_type:
          description: '"direct" or "code_execution"'
          type: string
        id:
          description: Unique identifier
          type: string
        mcp_client_name:
          description: MCP client name from InitializeRequest
          type: string
        mcp_client_version:
          description: MCP client version
          type: string
        mcp_session_id:
          description: MCP session identifier
          type: string
        metrics:
          $ref: '#/components/schemas/contracts.TokenMetrics'
        parent_call_id:
          description: Links nested calls to parent code_execution
          type: string
        request_id:
          description: Request correlation ID
          type: string
        response:
          description: Tool response (success only)
          type: object
        server_id:
          description: Server identity hash
          type: string
        server_name:
          description: Human-readable server name
          type: string
        timestamp:
          description: When the call was made
          type: string
        tool_name:
          description: Tool name (without server prefix)
          type: string
      type: object
    contracts.UpdateInfo:
      description: Update information (if available)
      properties:
        available:
          description: Whether an update is available
          type: boolean
        check_error:
          description: Error message if update check failed
          type: string
        checked_at:
          description: When the update check was performed
          type: string
        install_channel:
          description: Detected install channel (homebrew, dmg, deb, rpm, docker,
            go-install, windows-installer, tarball, unknown) — Spec 079 FR-008
          type: string
        is_prerelease:
          description: Whether the latest version is a prerelease
          type: boolean
        latest_version:
          description: Latest version available (e.g., "v1.2.3")
          type: string
        nudges_suppressed:
          description: UI surfaces must stay quiet (CI / non-interactive context);
            machine-readable fields still report the facts — Spec 079 FR-019
          type: boolean
        release_url:
          description: URL to the release page
          type: string
        update_command:
          description: One-line update command for the channel; only set when an update
            is available and the channel has one — Spec 079 FR-009
          type: string
      type: object
    contracts.UpdatePolicy:
      description: |-
        UpdatePolicy is the effective, hot-reloadable update policy (Spec 092
        FR-015). Always present: the `update` object above is omitted both when
        update checking is disabled AND when no check has produced a result
        yet, so its absence cannot tell a client whether it is allowed to run
        its own (e.g. Sparkle feed) check. This field states the answer.
      properties:
        channel:
          description: 'Channel is the tracked release channel: "stable" or "rc".'
          type: string
        enabled:
          description: |-
            Enabled is the effective automatic-check kill switch: update_check.enabled
            with MCPPROXY_DISABLE_AUTO_UPDATE=true winning over it. A user-initiated
            "Check for Updates" stays available regardless.
          type: boolean
        nudges_suppressed:
          description: |-
            NudgesSuppressed asks UI surfaces to stay quiet (CI / non-interactive)
            while machine-readable fields keep reporting the facts.
          type: boolean
      type: object
    contracts.UpstreamError:
      properties:
        error_message:
          type: string
        server_name:
          type: string
        timestamp:
          type: string
      type: object
    contracts.UsageAggregateResponse:
      properties:
        freshness_ms:
          description: age of the underlying snapshot in ms
          type: integer
        generated_at:
          type: string
        other:
          $ref: '#/components/schemas/contracts.UsageOtherBucket'
        timeline:
          items:
            $ref: '#/components/schemas/contracts.UsageTimeBucket'
          type: array
          uniqueItems: false
        token_source:
          description: '"bytes" (size-based proxy, FR-006)'
          type: string
        tokens_saved:
          description: echoed from ServerTokenMetrics (FR-007)
          type: integer
        tokens_saved_percentage:
          type: number
        tools:
          items:
            $ref: '#/components/schemas/contracts.UsageToolStat'
          type: array
          uniqueItems: false
        window:
          type: string
      type: object
    contracts.UsageOtherBucket:
      description: present only when the list was truncated to top-N
      properties:
        calls:
          type: integer
        tools_folded:
          type: integer
        total_resp_bytes:
          type: integer
      type: object
    contracts.UsageTimeBucket:
      properties:
        calls:
          type: integer
        errors:
          type: integer
        start:
          type: string
        total_resp_bytes:
          type: integer
      type: object
    contracts.UsageToolStat:
      properties:
        avg_req_bytes:
          description: null when no sized request calls
          type: integer
        avg_resp_bytes:
          description: null when sized_calls == 0 (only legacy 0-byte calls)
          type: integer
        blocked:
          type: integer
        calls:
          type: integer
        error_rate:
          type: number
        errors:
          type: integer
        last_used:
          type: string
        p50_ms:
          type: integer
        p95_ms:
          type: integer
        rejected:
          description: 'spec 093: shed by a concurrency limit; never executed, so
            excluded from calls/latency'
          type: integer
        server:
          type: string
        sized_calls:
          description: calls with known response size (basis for avg_resp_bytes)
          type: integer
        tool:
          type: string
        total_req_bytes:
          type: integer
        total_resp_bytes:
          type: integer
      type: object
    contracts.ValidateConfigResponse:
      properties:
        errors:
          items:
            $ref: '#/components/schemas/contracts.ValidationError'
          type: array
          uniqueItems: false
        valid:
          type: boolean
      type: object
    contracts.ValidationError:
      properties:
        field:
          type: string
        message:
          type: string
      type: object
    data:
      properties:
        data:
          $ref: '#/components/schemas/contracts.InfoResponse'
      type: object
    httpapi.AddServerRequest:
      properties:
        args:
          items:
            type: string
          type: array
          uniqueItems: false
        auto_approve_tool_changes:
          description: |-
            AutoApproveToolChanges is the per-server intent to auto-approve
            new/changed tools past the trust baseline (MCP-2930). Tri-state *bool:
            a nil pointer means "leave unchanged" on PATCH; a present value
            (including false) is applied. Mirrors config.ServerConfig's *bool
            semantics — do NOT collapse to a plain bool, or an omitted field would
            silently reset a previously-set value.
          type: boolean
        command:
          type: string
        enabled:
          type: boolean
        env:
          additionalProperties:
            type: string
          type: object
        expose_prompts:
          description: |-
            ExposePrompts is the per-server override for prompt aggregation (F9):
            whether this server's advertised MCP prompts are merged into mcpproxy's
            prompts/list. Tri-state *bool mirroring config.ServerConfig.ExposePrompts —
            a nil pointer means "leave unchanged" on PATCH (and "inherit the default
            aggregate behavior" on create); a present value (including false) is applied.
          type: boolean
        headers:
          additionalProperties:
            type: string
          type: object
        init_timeout:
          description: |-
            InitTimeout is the per-server MCP `initialize` handshake deadline override
            (MCP-3322 / GH #760), serialized as a duration string (e.g. "120s"). A nil
            pointer means "leave unchanged" on PATCH; a present value is applied.
            Mirrors config.ServerConfig.InitTimeout's *Duration tri-state.
          type: string
        isolation:
          $ref: '#/components/schemas/httpapi.IsolationRequest'
        max_concurrent_requests:
          description: |-
            MaxConcurrentRequests / QueueSize / QueueTimeout are the per-server
            concurrency overrides (spec 093 / GH #955, FR-020 scope (c)). Each is
            tri-state: a nil pointer means "leave unchanged" on PATCH and "inherit
            server_concurrency_defaults" on create; an explicit 0 disables that
            setting for this server; a positive value overrides it. Do NOT collapse
            them to plain values — an omitted field would then silently reset a
            configured limit.
          type: integer
        name:
          type: string
        protocol:
          type: string
        quarantined:
          type: boolean
        queue_size:
          type: integer
        queue_timeout:
          type: string
        reconnect_on_use:
          type: boolean
        trust_mode:
          description: |-
            TrustMode is the per-server trust tier (spec 086): "auto", "scan", or
            "manual". Empty means "leave unchanged" on PATCH (and inherit the migrated
            default on create). A non-empty value is applied to ServerConfig.TrustMode
            and resolved by EffectiveTrustMode (an unrecognized value fails closed to
            manual). This is the REST seam for changing the trust tier via
            POST/PATCH /api/v1/servers.
          type: string
        url:
          type: string
        working_dir:
          type: string
      type: object
    httpapi.CanonicalConfigPath:
      properties:
        description:
          description: Brief description
          type: string
        exists:
          description: Whether the file exists
          type: boolean
        format:
          description: Format identifier (e.g., "claude_desktop")
          type: string
        name:
          description: Display name (e.g., "Claude Desktop")
          type: string
        os:
          description: Operating system (darwin, windows, linux)
          type: string
        path:
          description: Full path to the config file
          type: string
      type: object
    httpapi.CanonicalConfigPathsResponse:
      properties:
        os:
          description: Current operating system
          type: string
        paths:
          description: List of canonical config paths
          items:
            $ref: '#/components/schemas/httpapi.CanonicalConfigPath'
          type: array
          uniqueItems: false
      type: object
    httpapi.ConnectConflictResponse:
      properties:
        action:
          description: already_exists | precondition_failed
          type: string
        data:
          $ref: '#/components/schemas/connect.ConnectResult'
        error:
          description: Human-readable message
          type: string
        success:
          description: Always false
          type: boolean
      type: object
    httpapi.ConnectRequest:
      properties:
        force:
          description: Overwrite existing entry
          type: boolean
        precondition_token:
          description: |-
            PreconditionToken is the opaque token from the preview this write was
            confirmed against (Spec 091 FR-005). When present, the core rechecks it
            at write time and responds 409 with action "precondition_failed" —
            writing nothing — if the config or the entry MCPProxy would write has
            drifted since; the caller then re-previews instead of retrying. Absent
            means exactly the pre-091 behavior. A replace-classified flow sends this
            TOGETHER with force=true: the token, not the absence of force, is the
            overwrite safety.
          type: string
        server_name:
          description: Defaults to "mcpproxy"
          type: string
      type: object
    httpapi.ImportFromPathRequest:
      properties:
        format:
          description: Optional format hint
          type: string
        path:
          description: File path to import from
          type: string
        rename:
          additionalProperties:
            type: string
          description: |-
            Rename maps a server name → new name. Applied after parsing so the
            caller can disambiguate cross-source name collisions (Spec 046 v2 —
            e.g. "mcpproxy" → "mcpproxy_claude_code"). Keys are matched against
            either the raw source name (OriginalName) or the sanitized name shown
            in the preview (Server.Name); these differ for names that need
            sanitizing (e.g. "Figma Desktop" → "Figma_Desktop"). Keys not present
            in the imported set are ignored.
          type: object
        server_names:
          description: 'Optional: import only these servers'
          items:
            type: string
          type: array
          uniqueItems: false
      type: object
    httpapi.ImportRequest:
      properties:
        content:
          description: Raw JSON or TOML content
          type: string
        format:
          description: Optional format hint
          type: string
        server_names:
          description: 'Optional: import only these servers'
          items:
            type: string
          type: array
          uniqueItems: false
      type: object
    httpapi.ImportResponse:
      properties:
        failed:
          items:
            $ref: '#/components/schemas/configimport.FailedServer'
          type: array
          uniqueItems: false
        format:
          type: string
        format_name:
          type: string
        imported:
          items:
            $ref: '#/components/schemas/httpapi.ImportedServerResponse'
          type: array
          uniqueItems: false
        skipped:
          items:
            $ref: '#/components/schemas/configimport.SkippedServer'
          type: array
          uniqueItems: false
        summary:
          $ref: '#/components/schemas/configimport.ImportSummary'
        warnings:
          items:
            type: string
          type: array
          uniqueItems: false
      type: object
    httpapi.ImportedServerResponse:
      properties:
        args:
          items:
            type: string
          type: array
          uniqueItems: false
        command:
          type: string
        fields_skipped:
          items:
            type: string
          type: array
          uniqueItems: false
        name:
          type: string
        original_name:
          type: string
        protocol:
          type: string
        source_format:
          type: string
        url:
          type: string
        warnings:
          items:
            type: string
          type: array
          uniqueItems: false
      type: object
    httpapi.IsolationRequest:
      description: |-
        Isolation carries per-server Docker isolation overrides (image,
        network_mode, extra_args, working_dir, enabled). A nil pointer
        means "do not touch isolation config"; an empty-but-present
        object on PATCH intentionally clears the overrides.
      properties:
        enabled:
          type: boolean
        extra_args:
          items:
            type: string
          type: array
          uniqueItems: false
        image:
          type: string
        network_mode:
          type: string
        working_dir:
          type: string
      type: object
    httpapi.OnboardingMarkRequest:
      properties:
        connect_step_status:
          description: |-
            ConnectStepStatus is one of: "", "completed", "skipped". Empty
            preserves the existing value. The stored enum is wider (Spec 080
            FR-001): a "skipped" request for a previously untouched connect step
            is upgraded server-side to "completed_external" when the install
            shows positive evidence of an external connection (Spec 080 FR-002).
            "completed_external" is NOT accepted from clients — it must never be
            persisted without that server-verified evidence (edge case: "never
            guess completed_external without positive evidence").
          type: string
        engaged:
          description: |-
            Engaged marks the wizard as engaged (completed or explicitly skipped).
            Once true, the wizard does not auto-show again.
          type: boolean
        mark_shown:
          description: MarkShown records the wizard's first display time if not already
            set.
          type: boolean
        server_step_status:
          description: |-
            ServerStepStatus is one of: "", "completed", "skipped". Empty
            preserves the existing value.
          type: string
      type: object
    httpapi.SetActiveProfileRequest:
      properties:
        active_profile:
          type: string
        profile:
          type: string
      type: object
    httpapi.UndoConnectRequest:
      properties:
        backup_name:
          description: |-
            BackupName is the bare filename (filepath.Base) of the backup returned as
            backup_path by the preceding connect — a name, never a path. Undo resolves
            the full path server-side by joining it with the client's own config
            directory, so a client-supplied value can never contribute a directory
            component (traversal is impossible by construction). Empty means the
            connect created the file (no prior file existed), so undo removes it.
          type: string
        server_name:
          description: Defaults to "mcpproxy"
          type: string
      type: object
    httpapi.UpdateFailureRequest:
      properties:
        stage:
          description: Stage is the failure stage of the update session.
          enum:
          - appcast
          - download
          - install
          - other
          type: string
      type: object
    management.BulkOperationResult:
      properties:
        errors:
          additionalProperties:
            type: string
          description: Map of server name to error message
          type: object
        failed:
          description: Number of failed operations
          type: integer
        successful:
          description: Number of successful operations
          type: integer
        total:
          description: Total servers processed
          type: integer
      type: object
    observability.HealthResponse:
      properties:
        components:
          items:
            $ref: '#/components/schemas/observability.HealthStatus'
          type: array
          uniqueItems: false
        status:
          description: '"healthy" or "unhealthy"'
          type: string
        timestamp:
          type: string
      type: object
    observability.HealthStatus:
      properties:
        error:
          type: string
        latency:
          type: string
        name:
          type: string
        status:
          description: '"healthy" or "unhealthy"'
          type: string
      type: object
    observability.ReadinessResponse:
      properties:
        components:
          items:
            $ref: '#/components/schemas/observability.HealthStatus'
          type: array
          uniqueItems: false
        status:
          description: '"ready" or "not_ready"'
          type: string
        timestamp:
          type: string
      type: object
    secureenv.EnvConfig:
      description: Environment configuration for secure variable filtering
      properties:
        allowed_system_vars:
          items:
            type: string
          type: array
          uniqueItems: false
        custom_vars:
          additionalProperties:
            type: string
          type: object
        enhance_path:
          description: Enable PATH enhancement for Launchd scenarios
          type: boolean
        forward_proxy_env:
          description: |-
            ForwardProxyEnv opts in to forwarding the ambient HTTP(S)/ALL/NO/FTP proxy
            environment variables to spawned upstream servers (MCP-2769). It is OFF by
            default and deliberately kept out of the AllowedSystemVars default list:
            proxy URLs frequently carry credentials (http://user:pass@proxy), so
            forwarding them to every stdio upstream is a credential-leak risk. When
            enabled, values are forwarded with their userinfo (credentials) redacted.
          type: boolean
        inherit_system_safe:
          type: boolean
      type: object
    telemetry.FeedbackContext:
      properties:
        arch:
          type: string
        connected_server_count:
          type: integer
        edition:
          type: string
        os:
          type: string
        routing_mode:
          type: string
        server_count:
          type: integer
        version:
          type: string
      type: object
    telemetry.FeedbackRequest:
      properties:
        category:
          description: bug, feature, other
          type: string
        context:
          $ref: '#/components/schemas/telemetry.FeedbackContext'
        email:
          type: string
        message:
          type: string
      type: object
    telemetry.FeedbackResponse:
      properties:
        error:
          type: string
        issue_url:
          type: string
        success:
          type: boolean
      type: object
  securitySchemes:
    ApiKeyAuth:
      description: API key authentication via query parameter. Use ?apikey=your-key
      in: query
      name: apikey
      type: apiKey
externalDocs:
  description: ""
  url: ""
info:
  contact:
    name: MCPProxy Support
    url: https://github.com/smart-mcp-proxy/mcpproxy-go
  description: |-
    MCPProxy REST API for managing MCP servers, tools, and diagnostics

    MCPProxy is a smart proxy for AI agents using the Model Context Protocol (MCP).
    It provides intelligent tool discovery, massive token savings, and built-in security quarantine.
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
  title: MCPProxy API
  version: "1.0"
openapi: 3.1.0
paths:
  /api/v1/activity:
    get:
      description: Returns paginated list of activity records with optional filtering
      parameters:
      - description: Filter by activity type(s), comma-separated for multiple (Spec
          024)
        in: query
        name: type
        schema:
          enum:
          - tool_call
          - policy_decision
          - quarantine_change
          - server_change
          - system_start
          - system_stop
          - internal_tool_call
          - config_change
          - preflight
          - prompt_get
          type: string
      - description: Filter by server name
        in: query
        name: server
        schema:
          type: string
      - description: Filter by tool name
        in: query
        name: tool
        schema:
          type: string
      - description: Filter by MCP transport session ID
        in: query
        name: session_id
        schema:
          type: string
      - description: Filter by work session (one client, one project, across reconnects)
        in: query
        name: work_session_id
        schema:
          type: string
      - description: Filter by status
        in: query
        name: status
        schema:
          enum:
          - success
          - error
          - blocked
          - rejected
          type: string
      - description: Filter by intent operation type (Spec 018)
        in: query
        name: intent_type
        schema:
          enum:
          - read
          - write
          - destructive
          type: string
      - description: Filter by HTTP request ID for log correlation (Spec 021)
        in: query
        name: request_id
        schema:
          type: string
      - description: 'Include successful call_tool_* internal tool calls (default:
          false, excluded to avoid duplicates)'
        in: query
        name: include_call_tool
        schema:
          type: boolean
      - description: Filter by sensitive data detection (true=has detections, false=no
          detections)
        in: query
        name: sensitive_data
        schema:
          type: boolean
      - description: Filter by specific detection type (e.g., 'aws_access_key', 'credit_card')
        in: query
        name: detection_type
        schema:
          type: string
      - description: Filter by severity level
        in: query
        name: severity
        schema:
          enum:
          - critical
          - high
          - medium
          - low
          type: string
      - description: Filter by agent token name (Spec 028)
        in: query
        name: agent
        schema:
          type: string
      - description: Filter by auth type (Spec 028)
        in: query
        name: auth_type
        schema:
          enum:
          - admin
          - agent
          type: string
      - description: Filter activities after this time (RFC3339)
        in: query
        name: start_time
        schema:
          type: string
      - description: Filter activities before this time (RFC3339)
        in: query
        name: end_time
        schema:
          type: string
      - description: Maximum records to return (1-100, default 50)
        in: query
        name: limit
        schema:
          type: integer
      - description: Pagination offset (default 0)
        in: query
        name: offset
        schema:
          type: integer
      - description: 'Omit arguments, response and metadata except a contextual whitelist
          (intent.reason, intent.operation_type, decision, reason, client_name, client_version)
          (default: false). For clients that render summary fields only; has_sensitive_data
          is still derived before metadata is dropped.'
        in: query
        name: exclude_payloads
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/data'
                properties:
                  data:
                    type: object
                  error:
                    type: string
                  request_id:
                    type: string
                  success:
                    type: boolean
                type: object
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Bad Request
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Internal Server Error
      security:
      - ApiKeyHeader: []
      - ApiKeyQuery: []
      summary: List activity records
      tags:
      - Activity
  /api/v1/activity/{id}:
    get:
      description: Returns full details for a single activity record
      parameters:
      - description: Activity record ID (ULID)
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/data'
                properties:
                  data:
                    type: object
                  error:
                    type: string
                  request_id:
                    type: string
                  success:
                    type: boolean
                type: object
          description: OK
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Unauthorized
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Not Found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Internal Server Error
      security:
      - ApiKeyHeader: []
      - ApiKeyQuery: []
      summary: Get activity record details
      tags:
      - Activity
  /api/v1/activity/export:
    get:
      description: Exports activity records in JSON Lines or CSV format for compliance
      parameters:
      - description: 'Export format: json (default) or csv'
        in: query
        name: format
        schema:
          type: string
      - description: Filter by activity type
        in: query
        name: type
        schema:
          type: string
      - description: Filter by server name
        in: query
        name: server
        schema:
          type: string
      - description: Filter by tool name
        in: query
        name: tool
        schema:
          type: string
      - description: Filter by MCP transport session ID
        in: query
        name: session_id
        schema:
          type: string
      - description: Filter by work session (one client, one project, across reconnects)
        in: query
        name: work_session_id
        schema:
          type: string
      - description: Filter by status
        in: query
        name: status
        schema:
          type: string
      - description: Filter activities after this time (RFC3339)
        in: query
        name: start_time
        schema:
          type: string
      - description: Filter activities before this time (RFC3339)
        in: query
        name: end_time
        schema:
          type: string
      - description: Maximum records to export (1-50000, default 10000)
        in: query
        name: limit
        schema:
          type: integer
      - description: Pagination offset (default 0)
        in: query
        name: offset
        schema:
          type: integer
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                type: string
            application/x-ndjson:
              schema:
                type: string
            text/csv:
              schema:
                type: string
          description: Streamed activity records
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Internal Server Error
      security:
      - ApiKeyHeader: []
      - ApiKeyQuery: []
      summary: Export activity records
      tags:
      - Activity
  /api/v1/activity/summary:
    get:
      description: Returns aggregated activity statistics for a time period
      parameters:
      - description: 'Time period: 1h, 24h (default), 7d, 30d'
        in: query
        name: period
        schema:
          type: string
      - description: 'Group by: server, tool (optional)'
        in: query
        name: group_by
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/data'
                properties:
                  data:
                    type: object
                  error:
                    type: string
                  request_id:
                    type: string
                  success:
                    type: boolean
                type: object
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Bad Request
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Internal Server Error
      security:
      - ApiKeyHeader: []
      - ApiKeyQuery: []
      summary: Get activity summary statistics
      tags:
      - Activity
  /api/v1/activity/usage:
    get:
      description: Returns the actor-owned usage aggregate (per-tool rollup + timeline
        + tokens-saved headline) for the Web UI usage graphs (Spec 069). Served from
        an in-memory snapshot — never a per-request full-log scan. Per-tool metrics
        are lifetime-cumulative; `window` scopes the timeline and filters the tool
        list to tools active within the span.
      parameters:
      - description: Time window for timeline + tool-list membership
        in: query
        name: window
        schema:
          enum:
          - 24h
          - 7d
          - all
          type: string
      - description: Filter to one server
        in: query
        name: server
        schema:
          type: string
      - description: Filter to one tool
        in: query
        name: tool
        schema:
          type: string
      - description: Filter to tools with activity of this status
        in: query
        name: status
        schema:
          enum:
          - success
          - error
          - blocked
          - rejected
          type: string
      - description: Top-N tools by sort key; remainder folded into 'other' (default
          20)
        in: query
        name: top
        schema:
          type: integer
      - description: Ranking key for the per-tool list
        in: query
        name: sort
        schema:
          enum:
          - calls
          - resp_bytes
          - error_rate
          - p95
          type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/data'
                properties:
                  data:
                    type: object
                  error:
                    type: string
                  request_id:
                    type: string
                  success:
                    type: boolean
                type: object
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Bad Request
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Unauthorized
      security:
      - ApiKeyHeader: []
      - ApiKeyQuery: []
      summary: Get usage statistics aggregate
      tags:
      - Activity
  /api/v1/annotations/coverage:
    get:
      description: Reports how many upstream tools have MCP annotations vs don't,
        broken down by server
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Annotation coverage report
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get annotation coverage report
      tags:
      - annotations
  /api/v1/code/scripts:
    get:
      description: 'List the stored scripts available to the code_execution tool.
        Scripts are `<name>.js` / `<name>.ts` files in the `scripts/` directory next
        to the active configuration file. Entries are advisory: `ok` scripts are invocable,
        `ambiguous` names have both extensions, and `invalid` ones report why (empty,
        oversized, unreadable, non-regular). Read-only — there is no write surface
        for stored scripts.'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Stored scripts and the directory they were read from
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: List stored code-execution scripts
      tags:
      - code
  /api/v1/config:
    get:
      description: Retrieves the current MCPProxy configuration including all server
        definitions, global settings, and runtime parameters
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.GetConfigResponse'
          description: Configuration retrieved successfully
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to get configuration
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get current configuration
      tags:
      - config
    patch:
      description: Deep-merges only the fields present in the request body onto the
        live in-memory configuration and routes the result through the existing apply
        pipeline (validation, change detection, disk persistence, hot-reload). Fields
        the client omits — including masked secrets such as `api_key` and secret request
        headers — are preserved verbatim. Nested objects are merged recursively; arrays
        and scalars replace wholesale.
      requestBody:
        content:
          application/json:
            schema:
              type: object
        description: Partial configuration with only the fields to change
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ConfigApplyResult'
          description: Configuration patch applied (inspect validation_errors for
            rejected values)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Invalid JSON payload or empty patch
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate configuration)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to read or apply configuration
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Partially update configuration
      tags:
      - config
  /api/v1/config/apply:
    post:
      description: Applies a new MCPProxy configuration. Validates and persists the
        configuration to disk. Some changes apply immediately, while others may require
        a restart. Returns detailed information about applied changes and restart
        requirements.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/config.Config'
        description: Configuration to apply
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ConfigApplyResult'
          description: Configuration applied successfully with change details
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Invalid JSON payload
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate configuration)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to apply configuration
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Apply configuration
      tags:
      - config
  /api/v1/config/docker-isolation:
    patch:
      description: Convenience endpoint to flip `docker_isolation.enabled` without
        resending the full config. Persists to disk via the existing config writer
        — the file watcher then hot-reloads the change. Returns the new state and
        whether a restart is required for existing connections to pick it up.
      requestBody:
        content:
          application/json:
            schema:
              properties:
                enabled:
                  type: boolean
              type: object
        description: New isolation state
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ConfigApplyResult'
          description: Isolation toggle applied
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Invalid JSON payload
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate configuration)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to apply configuration
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Toggle global Docker isolation
      tags:
      - config
  /api/v1/config/validate:
    post:
      description: Validates a provided MCPProxy configuration without applying it.
        Checks for syntax errors, invalid server definitions, conflicting settings,
        and other configuration issues.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/config.Config'
        description: Configuration to validate
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ValidateConfigResponse'
          description: Configuration validation result
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Invalid JSON payload
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Validation failed
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Validate configuration
      tags:
      - config
  /api/v1/connect:
    get:
      description: |-
        Returns the connection status for all known MCP client applications.
        Each entry indicates whether the client config file exists and whether
        MCPProxy is currently registered in it.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: List of ClientStatus objects
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: List client connection status
      tags:
      - connect
  /api/v1/connect/{client}:
    delete:
      description: |-
        Remove the MCPProxy entry from the specified client's configuration file.
        Creates a backup of the existing config before modifying.
      parameters:
      - description: Client ID (claude-code, claude-desktop, cursor, windsurf, vscode,
          codex, gemini, opencode)
        in: path
        name: client
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/httpapi.ConnectRequest'
        description: Optional parameters (server_name)
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: ConnectResult
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Permission denied (macOS App-Data block)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unknown client or entry not found
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Service unavailable
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Disconnect MCPProxy from a client
      tags:
      - connect
    get:
      description: |-
        Resolves one client's status by reading its config file on demand.
        This is the only Connect endpoint that opens a client config file, so
        on macOS it is the sole place an App-Data privacy prompt may legitimately
        appear (scoped to this user action). Resolves access_state to
        accessible|absent|denied|malformed and populates remediation when denied.
      parameters:
      - description: Client ID (claude-code, claude-desktop, cursor, windsurf, vscode,
          codex, gemini, opencode)
        in: path
        name: client
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: ClientStatus
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unknown client
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Service unavailable
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get a single client's connection status (on-demand)
      tags:
      - connect
    post:
      description: |-
        Register MCPProxy as an MCP server in the specified client's configuration file.
        Creates a backup of the existing config before modifying.
        Optionally accepts precondition_token from a preview (Spec 091): when supplied,
        the core rechecks the raw pre-write state and the entry it would write, and
        refuses a drifted write with 409 before taking any backup. The 409 body's
        action discriminates the two conflict kinds: "precondition_failed" (stale
        preview — re-preview, do not retry) vs "already_exists" (entry present — pass
        force=true). force=true never rescues a stale token.
      parameters:
      - description: Client ID (claude-code, claude-desktop, cursor, windsurf, vscode,
          codex, gemini, opencode)
        in: path
        name: client
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/httpapi.ConnectRequest'
        description: Optional connection parameters (server_name, force, precondition_token)
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: ConnectResult
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Permission denied (macOS App-Data block)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unknown client
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/httpapi.ConnectConflictResponse'
          description: 'Conflict: action=already_exists (use force=true) or action=precondition_failed
            (preview is stale; re-preview)'
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Service unavailable
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Connect MCPProxy to a client
      tags:
      - connect
  /api/v1/connect/{client}/preview:
    get:
      description: |-
        Returns the exact entry a subsequent connect would add to the client's
        config — target path, server key, entry name, and entry contents — WITHOUT
        modifying the file or creating a backup (Spec 078 US1). The embedded API key
        is masked in the payload; contains_api_key flags that a credential is written.
        entry_exists distinguishes a create from an overwrite of a same-named entry.
        Reads the config on demand to classify create-vs-overwrite, so on macOS this
        may raise an App-Data privacy prompt; a denial returns 403 + remediation.
        Spec 091 adds three fields: existing_entry_summary (present only when
        entry_exists — a sanitized, non-secret projection of the entry being replaced:
        its name, type, endpoint with query/userinfo stripped, command, and header and
        env NAMES, never values); precondition_token (always present — an opaque keyed
        digest of the raw pre-write state and the pending entry, echoed back on POST
        connect to detect drift); and connect_refusal (present when the write would
        refuse regardless of intent, e.g. a non-create-capable client with no config —
        treat its presence as "Connect unavailable").
      parameters:
      - description: Client ID (claude-code, claude-desktop, cursor, windsurf, vscode,
          codex, gemini, opencode)
        in: path
        name: client
        required: true
        schema:
          type: string
      - description: Entry name to preview (defaults to mcpproxy); mirror the value
          passed to POST connect
        in: query
        name: server_name
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: ConnectPreview
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Permission denied (macOS App-Data block)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unknown client
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Service unavailable
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Preview the change a connect would make (no write)
      tags:
      - connect
  /api/v1/connect/{client}/undo:
    post:
      description: |-
        Reverts the connect that produced the named backup (Spec 078 US3):
        restores the client config byte-for-byte from that backup, or — when
        backup_name is empty because the connect created the file — deletes the
        created file. backup_name is the bare filename of the backup the connect
        returned (never a path); undo resolves the full path server-side inside
        the client's own config directory, so a client value cannot escape it.
        Refuses with 409 when the config changed since the connect (undo never
        clobbers later edits; use DELETE /connect/{client} for a surgical entry
        removal instead). Takes its own safety backup first; its path is returned
        as backup_path in the result.
      parameters:
      - description: Client ID (claude-code, claude-desktop, cursor, windsurf, vscode,
          codex, gemini, opencode)
        in: path
        name: client
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/httpapi.UndoConnectRequest'
        description: Undo parameters (server_name, backup_name = the bare filename
          of the backup the preceding connect returned)
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: ConnectResult (action restored|deleted)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request (e.g. backup_name is a path, or not a backup of
            this client's config)
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Permission denied (macOS App-Data block)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unknown client or backup no longer exists
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Config changed since connect; undo refused
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Service unavailable
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Undo a connect, restoring the pre-connect config
      tags:
      - connect
  /api/v1/diagnostics:
    get:
      description: Get comprehensive health diagnostics including upstream errors,
        OAuth requirements, missing secrets, and Docker status
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.Diagnostics'
          description: Health diagnostics
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get health diagnostics
      tags:
      - diagnostics
  /api/v1/docker/status:
    get:
      description: Retrieve current Docker availability and recovery status
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Docker status information
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get Docker status
      tags:
      - docker
  /api/v1/doctor:
    get:
      description: Get comprehensive health diagnostics including upstream errors,
        OAuth requirements, missing secrets, and Docker status
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.Diagnostics'
          description: Health diagnostics
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get health diagnostics
      tags:
      - diagnostics
  /api/v1/feedback:
    post:
      description: Submit a bug report, feature request, or general feedback
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/telemetry.FeedbackRequest'
        description: Feedback request
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/telemetry.FeedbackResponse'
          description: OK
        "400":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                type: object
          description: Bad Request
        "429":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                type: object
          description: Too Many Requests
        "500":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                type: object
          description: Internal Server Error
      security:
      - ApiKeyAuth: []
      summary: Submit feedback
      tags:
      - feedback
  /api/v1/index/search:
    get:
      description: Search across all upstream MCP server tools using BM25 keyword
        search
      parameters:
      - description: Search query
        in: query
        name: q
        required: true
        schema:
          type: string
      - description: Maximum number of results
        in: query
        name: limit
        schema:
          default: 10
          maximum: 100
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SearchToolsResponse'
          description: Search results
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request (missing query parameter)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Search for tools
      tags:
      - tools
  /api/v1/info:
    get:
      description: |-
        Get essential server metadata including version, web UI URL, endpoint addresses, and update availability
        This endpoint is designed for tray-core communication and version checking
        Use refresh=true query parameter to force an immediate update check against GitHub
        The launched_by field reports durable launch provenance ("tray", "installer", or "" for user-launched/unknown)
      parameters:
      - description: Force immediate update check against GitHub
        in: query
        name: refresh
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/data'
                properties:
                  data:
                    type: object
                  error:
                    type: string
                  request_id:
                    type: string
                  success:
                    type: boolean
                type: object
          description: Server information with optional update info
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get server information
      tags:
      - status
  /api/v1/onboarding/mark:
    post:
      description: |-
        Updates wizard engagement and per-step status. Once engaged is
        true, the wizard does not auto-show again, even if state regresses.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/httpapi.OnboardingMarkRequest'
        description: Mark request
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Updated OnboardingStateResponse
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Service unavailable
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Mark onboarding wizard state (Spec 046)
      tags:
      - onboarding
  /api/v1/onboarding/state:
    get:
      description: |-
        Returns the wizard engagement record alongside live predicates
        (whether any client is connected, whether any server is configured),
        plus a derived ShouldShowWizard flag the frontend can rely on.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: OnboardingStateResponse
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Service unavailable
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get onboarding wizard state and predicates (Spec 046)
      tags:
      - onboarding
  /api/v1/preflight:
    post:
      description: 'Deterministic, side-effect-free availability check for a caller-supplied
        list of tool IDs (Spec 098). Performs zero upstream calls and mutates no runtime
        state. HTTP status reports whether the CHECK executed: a fully blocked set
        is still 200, with the availability verdict in the body. Every executed preflight
        writes an activity record before the response is returned.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/contracts.PreflightRequest'
        description: Tool IDs (1-100 before dedup), optional profile, annotation policy
          filters and wait budget
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/data'
                properties:
                  data:
                    type: object
                  error:
                    type: string
                  request_id:
                    type: string
                  success:
                    type: boolean
                type: object
          description: Preflight verdict and per-tool results
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Validation error (malformed, oversized, doubled or unknown-field
            body; empty or oversized tool list; conflicting duplicate pins; unknown
            profile; wait_ms out of range)
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Missing or invalid credentials
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.APIResponse'
          description: Runtime unavailable, evaluator infrastructure read failure,
            or the activity record could not be persisted
      security:
      - ApiKeyHeader: []
      - ApiKeyQuery: []
      summary: Preflight required tools
      tags:
      - tools
  /api/v1/profiles:
    get:
      description: List all configured profiles with their effective servers and indexed
        tool count (Profiles v2). A profile scopes tool discovery and calls to a named
        subset of upstream servers.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Profile list
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Configuration unavailable
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: List configured profiles
      tags:
      - profiles
  /api/v1/profiles/active:
    get:
      description: 'Get the server-level default active profile used by UI surfaces
        (Web UI / tray). Empty string means "all servers". Note: within a live MCP
        session, the set_profile tool selection takes precedence over this default.'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Active profile
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get the default active profile
      tags:
      - profiles
    put:
      description: Set the server-level default active profile for UI surfaces. The
        slug must match a configured profile; pass an empty string to clear. This
        does not affect live MCP sessions, which use the set_profile tool.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/httpapi.SetActiveProfileRequest'
        description: Profile slug to activate (empty clears)
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Active profile updated
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Invalid request body
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unknown profile
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Set the default active profile
      tags:
      - profiles
  /api/v1/registries:
    get:
      description: Retrieves list of all MCP server registries that can be browsed
        for discovering and installing new upstream servers. Includes registry metadata,
        server counts, and API endpoints.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.GetRegistriesResponse'
          description: Registries retrieved successfully
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to list registries
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: List available MCP server registries
      tags:
      - registries
    post:
      description: Adds a generic modelcontextprotocol/registry v0.1 https endpoint
        as a custom registry (MCP-866). The source is always tagged custom/unverified,
        so every server discovered through it lands quarantined and can never skip
        quarantine.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/contracts.AddRegistrySourceRequest'
        description: Registry source (https url + optional protocol/id/name)
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Registry source added
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: invalid_registry_url
        "403":
          content:
            application/json:
              schema:
                oneOf:
                - $ref: '#/components/schemas/contracts.ErrorResponse'
                - $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate registries)
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: registry_shadows_builtin | duplicate_registry
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Add a user-supplied registry source
      tags:
      - registries
  /api/v1/registries/{id}:
    delete:
      description: Removes a custom/unverified registry previously added via add-source
        (MCP-1057). Built-in registries are refused with registry_shadows_builtin;
        an unknown id yields registry_not_found. The change is persisted copy-on-write.
      parameters:
      - description: Registry ID
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Registry source removed
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Registry ID is required
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: registries_locked
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: registry_not_found
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: registry_shadows_builtin
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Remove a user-added custom registry source
      tags:
      - registries
    put:
      description: 'Updates a custom registry previously added via add-source (MCP-1072):
        name, url, servers-url. Empty fields are left unchanged. Built-in registries
        are refused with registry_shadows_builtin; an unknown id yields registry_not_found;
        a non-https url yields invalid_registry_url. The change is persisted copy-on-write.'
      parameters:
      - description: Registry ID
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/contracts.EditRegistrySourceRequest'
        description: Fields to update (name/url/servers_url; empty = unchanged)
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Registry source updated
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Registry ID is required | invalid_registry_url
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: registries_locked
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: registry_not_found
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: registry_shadows_builtin
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Edit a user-added custom registry source
      tags:
      - registries
  /api/v1/registries/{id}/refresh:
    post:
      description: Invalidates the cached server lists for a registry so the next
        search re-fetches fresh data from the source (spec 070 FR-007). Returns how
        many cache entries were dropped.
      parameters:
      - description: Registry ID
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.RefreshRegistryResponse'
          description: Registry cache refreshed
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Registry ID is required
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to refresh registry cache
      summary: Refresh a registry's cached server list
      tags:
      - registries
  /api/v1/registries/{id}/servers:
    get:
      description: Searches for MCP servers within a specific registry by keyword
        or tag. Returns server metadata including installation commands, source code
        URLs, and npm package information for easy discovery and installation.
      parameters:
      - description: Registry ID
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: Search query keyword
        in: query
        name: q
        schema:
          type: string
      - description: Filter by tag
        in: query
        name: tag
        schema:
          type: string
      - description: Maximum number of results (default 10)
        in: query
        name: limit
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SearchRegistryServersResponse'
          description: Servers retrieved successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Registry ID required
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to search servers
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Search MCP servers in a registry
      tags:
      - registries
  /api/v1/registries/{id}/servers/{serverId}/add:
    post:
      description: Resolves a registry server reference server-side, re-derives a
        validated config, and persists it quarantined (spec 070 keystone). The client
        never sends a config blob — command/args/url and the quarantine flag are derived
        from the registry entry, not the request.
      parameters:
      - description: Registry ID
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: Server ID within the registry
        in: path
        name: serverId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/contracts.AddFromRegistryRequest'
        description: Optional overrides (name, env, enabled)
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Server added (quarantined)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: no_install_info | missing_required_input | duplicate_name
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot add servers)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: registry_not_found | server_not_found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Add an upstream server from a registry reference
      tags:
      - registries
  /api/v1/routing:
    get:
      description: Get the current routing mode and available MCP endpoints
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Routing mode information
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get routing mode information
      tags:
      - status
  /api/v1/secrets:
    post:
      description: Stores a secret value in the operating system's secure keyring.
        The secret can then be referenced in configuration using ${keyring:secret-name}
        syntax. Automatically notifies runtime to restart affected servers.
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties: {}
                type: object
          description: Secret stored successfully with reference syntax
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Invalid JSON payload, missing name/value, or unsupported type
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "405":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Method not allowed
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Secret resolver not available or failed to store secret
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Store a secret in OS keyring
      tags:
      - secrets
  /api/v1/secrets/{name}:
    delete:
      description: Deletes a secret from the operating system's secure keyring. Automatically
        notifies runtime to restart affected servers. Only keyring type is supported
        for security.
      parameters:
      - description: Name of the secret to delete
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Secret type (only 'keyring' supported, defaults to 'keyring')
        in: query
        name: type
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties: {}
                type: object
          description: Secret deleted successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Missing secret name or unsupported type
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "405":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Method not allowed
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Secret resolver not available or failed to delete secret
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Delete a secret from OS keyring
      tags:
      - secrets
  /api/v1/servers:
    get:
      description: Get a list of all configured upstream MCP servers with their connection
        status and statistics
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.GetServersResponse'
          description: Server list with statistics
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: List all upstream MCP servers
      tags:
      - servers
    post:
      description: Add a new MCP upstream server to the configuration. New servers
        are quarantined by default for security.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/httpapi.AddServerRequest'
        description: Server configuration
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ServerActionResponse'
          description: Server added successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request - invalid configuration
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Conflict - server with this name already exists
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Add a new upstream server
      tags:
      - servers
  /api/v1/servers/{id}:
    delete:
      description: Remove an MCP upstream server from the configuration. This stops
        the server if running and removes it from config.
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ServerActionResponse'
          description: Server removed successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Remove an upstream server
      tags:
      - servers
    patch:
      description: Update specific fields of an existing upstream MCP server configuration.
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/httpapi.AddServerRequest'
        description: Fields to update (all optional)
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Server updated successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request - no fields or invalid body
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Partially update an upstream server
      tags:
      - servers
  /api/v1/servers/{id}/config-to-secret:
    post:
      description: Atomically reads the real value from the server config, stores
        it in the OS keyring, and rewrites the config field to `${keyring:<name>}`.
        Unblocks the UI's Convert-to-secret affordance for values the API redacts
        on the read path.
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties: {}
                type: object
          description: Secret stored, config updated with reference
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad scope/key/secret_name, or value is already a reference
            / empty
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server or key not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Secret resolver or config update failed
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Convert a header / env value to a keyring secret
      tags:
      - servers
  /api/v1/servers/{id}/disable:
    post:
      description: Disable a specific upstream MCP server
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ServerActionResponse'
          description: Server disabled successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Disable an upstream server
      tags:
      - servers
  /api/v1/servers/{id}/discover-tools:
    post:
      description: Manually trigger tool discovery and indexing for a specific upstream
        MCP server. This forces an immediate refresh of the server's tool cache.
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ServerActionResponse'
          description: Tool discovery triggered successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request (missing server ID)
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot discover tools)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to discover tools
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Discover tools for a specific server
      tags:
      - servers
  /api/v1/servers/{id}/enable:
    post:
      description: Enable a specific upstream MCP server
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ServerActionResponse'
          description: Server enabled successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Enable an upstream server
      tags:
      - servers
  /api/v1/servers/{id}/login:
    post:
      description: Initiate OAuth authentication flow for a specific upstream MCP
        server. Returns structured OAuth start response with correlation ID for tracking.
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.OAuthStartResponse'
          description: OAuth login initiated successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.OAuthFlowError'
          description: OAuth error (client_id required, DCR failed, etc.)
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Trigger OAuth login for server
      tags:
      - servers
  /api/v1/servers/{id}/logout:
    post:
      description: Clear OAuth authentication token and disconnect a specific upstream
        MCP server. The server will need to re-authenticate before tools can be used
        again.
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ServerActionResponse'
          description: OAuth logout completed successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request (missing server ID)
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (management disabled or read-only mode)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Clear OAuth token and disconnect server
      tags:
      - servers
  /api/v1/servers/{id}/logs:
    get:
      description: Retrieve log entries for a specific upstream MCP server
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: Number of log lines to retrieve
        in: query
        name: tail
        schema:
          default: 100
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.GetServerLogsResponse'
          description: Server logs retrieved successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request (missing server ID)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get server logs
      tags:
      - servers
  /api/v1/servers/{id}/quarantine:
    post:
      description: Place a specific upstream MCP server in quarantine to prevent tool
        execution
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ServerActionResponse'
          description: Server quarantined successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request (missing server ID)
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Quarantine a server
      tags:
      - servers
  /api/v1/servers/{id}/refresh:
    post:
      description: Re-discover and re-index a specific upstream MCP server's tools
        without changing any security state. Alias of discover-tools, named for the
        upstream_servers 'refresh' operation; use it to make just-approved tools searchable
        immediately.
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ServerActionResponse'
          description: Tool refresh triggered successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request (missing server ID)
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot refresh)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to refresh tools
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Refresh a server's tools
      tags:
      - servers
  /api/v1/servers/{id}/restart:
    post:
      description: Restart the connection to a specific upstream MCP server
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ServerActionResponse'
          description: Server restarted successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Restart an upstream server
      tags:
      - servers
  /api/v1/servers/{id}/tool-calls:
    get:
      description: Retrieves tool call history filtered by upstream server ID. Returns
        recent tool executions for the specified server including timestamps, arguments,
        results, and errors. Useful for server-specific debugging and monitoring.
      parameters:
      - description: Upstream server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: Maximum number of records to return (1-100, default 50)
        in: query
        name: limit
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.GetServerToolCallsResponse'
          description: Server tool calls retrieved successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server ID required
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "405":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Method not allowed
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to get server tool calls
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get tool call history for specific server
      tags:
      - tool-calls
  /api/v1/servers/{id}/tools:
    get:
      description: Retrieve all available tools for a specific upstream MCP server
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.GetServerToolsResponse'
          description: Server tools retrieved successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request (missing server ID)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get tools for a server
      tags:
      - servers
  /api/v1/servers/{id}/tools/block:
    post:
      description: Atomically approves AND disables the given tools (or all pending/changed
        tools when block_all=true) for a server. The approve and disable land in a
        single write per tool, so a tool is never left in the approved+enabled state.
        The "blocked" field counts tools actually blocked.
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Block result
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Block (approve+disable) tools for a server
      tags:
      - servers
  /api/v1/servers/{id}/tools/disable_all:
    post:
      description: Bulk-toggles every known tool of a server. The "changed" field
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Operation result
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Enable or disable all tools for a server
      tags:
      - servers
  /api/v1/servers/{id}/tools/enable_all:
    post:
      description: Bulk-toggles every known tool of a server. The "changed" field
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Operation result
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Enable or disable all tools for a server
      tags:
      - servers
  /api/v1/servers/{id}/unquarantine:
    post:
      description: Remove a specific upstream MCP server from quarantine to allow
        tool execution
      parameters:
      - description: Server ID or name
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ServerActionResponse'
          description: Server unquarantined successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request (missing server ID)
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Server not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Unquarantine a server
      tags:
      - servers
  /api/v1/servers/disable_all:
    post:
      description: Disable all configured upstream MCP servers with partial failure
        handling
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/management.BulkOperationResult'
          description: Bulk disable results with success/failure counts
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (management disabled)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Disable all servers
      tags:
      - servers
  /api/v1/servers/enable_all:
    post:
      description: Enable all configured upstream MCP servers with partial failure
        handling
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/management.BulkOperationResult'
          description: Bulk enable results with success/failure counts
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (management disabled)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Enable all servers
      tags:
      - servers
  /api/v1/servers/import:
    post:
      description: Import MCP server configurations from a Claude Desktop, Claude
        Code, Cursor IDE, Codex CLI, or Gemini CLI configuration file
      parameters:
      - description: If true, return preview without importing
        in: query
        name: preview
        schema:
          type: boolean
      - description: Force format (claude-desktop, claude-code, cursor, codex, gemini)
        in: query
        name: format
        schema:
          type: string
      - description: Comma-separated list of server names to import
        in: query
        name: server_names
        schema:
          type: string
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: file
        description: Configuration file to import
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/httpapi.ImportResponse'
          description: Import result
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request - invalid file or format
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Import servers from uploaded configuration file
      tags:
      - servers
  /api/v1/servers/import/json:
    post:
      description: Import MCP server configurations from raw JSON or TOML content
        (useful for pasting configurations)
      parameters:
      - description: If true, return preview without importing
        in: query
        name: preview
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/httpapi.ImportRequest'
        description: Import request with content
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/httpapi.ImportResponse'
          description: Import result
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request - invalid content or format
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Import servers from JSON/TOML content
      tags:
      - servers
  /api/v1/servers/import/path:
    post:
      description: Import MCP server configurations by reading a file from the server's
        filesystem
      parameters:
      - description: If true, return preview without importing
        in: query
        name: preview
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/httpapi.ImportFromPathRequest'
        description: Import request with file path
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/httpapi.ImportResponse'
          description: Import result
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request - invalid path or format
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: File not found
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Import servers from a file path
      tags:
      - servers
  /api/v1/servers/import/paths:
    get:
      description: Returns well-known configuration file paths for supported formats
        with existence check
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/httpapi.CanonicalConfigPathsResponse'
          description: Canonical config paths
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get canonical config file paths
      tags:
      - servers
  /api/v1/servers/reconnect:
    post:
      description: Force reconnection to all upstream MCP servers
      parameters:
      - description: Reason for reconnection
        in: query
        name: reason
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ServerActionResponse'
          description: All servers reconnected successfully
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (agent tokens cannot mutate servers)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Reconnect all servers
      tags:
      - servers
  /api/v1/servers/restart_all:
    post:
      description: Restart all configured upstream MCP servers sequentially with partial
        failure handling
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/management.BulkOperationResult'
          description: Bulk restart results with success/failure counts
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Forbidden (management disabled)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Restart all servers
      tags:
      - servers
  /api/v1/sessions:
    get:
      description: Retrieves paginated list of active and recent MCP client sessions.
        Each session represents a connection from an MCP client to MCPProxy, tracking
        initialization time, tool calls, and connection status.
      parameters:
      - description: Maximum number of sessions to return (1-100, default 10)
        in: query
        name: limit
        schema:
          type: integer
      - description: Number of sessions to skip for pagination (default 0)
        in: query
        name: offset
        schema:
          type: integer
      - description: Filter by session status
        in: query
        name: status
        schema:
          enum:
          - active
          - closed
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.GetSessionsResponse'
          description: Sessions retrieved successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Invalid status filter
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "405":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Method not allowed
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to get sessions
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get active MCP sessions
      tags:
      - sessions
  /api/v1/sessions/{id}:
    get:
      description: Retrieves detailed information about a specific MCP client session
        including initialization parameters, connection status, tool call count, and
        activity timestamps.
      parameters:
      - description: Session ID
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.GetSessionDetailResponse'
          description: Session details retrieved successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Session ID required
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Session not found
        "405":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Method not allowed
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get MCP session details by ID
      tags:
      - sessions
  /api/v1/stats/tokens:
    get:
      description: Retrieve token savings statistics across all servers and sessions
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Token statistics
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get token savings statistics
      tags:
      - stats
  /api/v1/status:
    get:
      description: Get comprehensive server status including running state, listen
        address, upstream statistics, and timestamp
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Server status information
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get server status
      tags:
      - status
  /api/v1/telemetry/payload:
    get:
      description: Render the exact JSON heartbeat payload that mcpproxy would next
        send to the telemetry endpoint, without making a network call. Counters in
        the payload reflect the current in-memory state. Spec 042.
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Telemetry heartbeat payload
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Telemetry service unavailable
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Preview next telemetry heartbeat payload
      tags:
      - telemetry
  /api/v1/telemetry/update-failure:
    post:
      description: |-
        Records one terminal update-session failure, identified only by its
        stage (appcast, download, install, other). The body carries no error
        text, URL, or version — the stage is the only value transmitted.
        Returns 204 both when the occurrence was durably persisted and when
        telemetry is inactive at event time (config opt-out, environment
        opt-out, CI, or dev build), in which case nothing is recorded.
        Callers cannot and need not distinguish the two.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/httpapi.UpdateFailureRequest'
        description: Update failure stage
        required: true
      responses:
        "204":
          description: Accepted (recorded, or a deliberate no-op while telemetry is
            inactive)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Malformed body, unknown field, trailing value, or stage outside
            the closed set
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Persistence failure
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Record a desktop auto-update failure occurrence (Spec 095)
      tags:
      - telemetry
  /api/v1/tool-calls:
    get:
      description: Retrieves paginated tool call history across all upstream servers
        or filtered by session ID. Includes execution timestamps, arguments, results,
        and error information for debugging and auditing.
      parameters:
      - description: Maximum number of records to return (1-100, default 50)
        in: query
        name: limit
        schema:
          type: integer
      - description: Number of records to skip for pagination (default 0)
        in: query
        name: offset
        schema:
          type: integer
      - description: Filter tool calls by MCP session ID
        in: query
        name: session_id
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.GetToolCallsResponse'
          description: Tool calls retrieved successfully
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "405":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Method not allowed
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to get tool calls
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get tool call history
      tags:
      - tool-calls
  /api/v1/tool-calls/{id}:
    get:
      description: Retrieves detailed information about a specific tool call execution
        including full request arguments, response data, execution time, and any errors
        encountered.
      parameters:
      - description: Tool call ID
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.GetToolCallDetailResponse'
          description: Tool call details retrieved successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Tool call ID required
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Tool call not found
        "405":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Method not allowed
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Get tool call details by ID
      tags:
      - tool-calls
  /api/v1/tool-calls/{id}/replay:
    post:
      description: Re-executes a previous tool call with optional modified arguments.
        Useful for debugging and testing tool behavior with different inputs. Creates
        a new tool call record linked to the original.
      parameters:
      - description: Original tool call ID to replay
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/contracts.ReplayToolCallRequest'
        description: Optional modified arguments for replay
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ReplayToolCallResponse'
          description: Tool call replayed successfully
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Tool call ID required or invalid JSON payload
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Unauthorized - missing or invalid API key
        "405":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Method not allowed
        "429":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Shed by a concurrency limit (Retry-After header carries the
            wait hint)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Failed to replay tool call
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Replay a tool call
      tags:
      - tool-calls
  /api/v1/tools:
    get:
      description: 'Consolidated, read-only listing of all tools from every configured
        server (including disabled servers and disabled/config-denied tools), enriched
        with approval state and 30-day usage. Backs the global Tools page and the
        CLI global `tools list` (spec 050, issue #437).'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.GlobalToolsResponse'
          description: All tools across all servers
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Could not enumerate servers
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: List every tool across all servers
      tags:
      - tools
  /api/v1/tools/call:
    post:
      description: Execute a tool on an upstream MCP server (wrapper around MCP tool
        calls)
      requestBody:
        content:
          application/json:
            schema:
              properties:
                arguments:
                  type: object
                tool_name:
                  type: string
              type: object
        description: Tool call request with tool name and arguments
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.SuccessResponse'
          description: Tool call result
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Bad request (invalid payload or missing tool name)
        "429":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Shed by a concurrency limit (Retry-After header carries the
            wait hint)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/contracts.ErrorResponse'
          description: Internal server error or tool execution failure
      security:
      - ApiKeyAuth: []
      - ApiKeyQuery: []
      summary: Call a tool
      tags:
      - tools
  /healthz:
    get:
      description: Get comprehensive health status including all component health
        (Kubernetes-compatible liveness probe)
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/observability.HealthResponse'
          description: Service is healthy
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/observability.HealthResponse'
          description: Service is unhealthy
      summary: Get health status
      tags:
      - health
  /readyz:
    get:
      description: Get readiness status including all component readiness checks (Kubernetes-compatible
        readiness probe)
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/observability.ReadinessResponse'
          description: Service is ready
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/observability.ReadinessResponse'
          description: Service is not ready
      summary: Get readiness status
      tags:
      - health
