{
  "browserless": {
    "host": "127.0.0.1",
    "port": 25130,
    "protocol": "http",
    "token": null,
    "timeout": 30000,
    "concurrent": 5
  },
  "notes": [
    "Live endpoint is the pitchfork daemon itvx-browserless on 127.0.0.1:25130.",
    "Token is NOT stored here: server token lives in 0600 /home/toxic/.browserless/.env;",
    "MCP clients pass it via initialize_browserless or BROWSERLESS_TOKEN env (see server/.env.example). NEVER commit a token."
  ],
  "workingFeatures": [
    "content",
    "pdf"
  ],
  "testResults": {
    "content": true,
    "pdf": true,
    "function": false,
    "export": false,
    "performance": false
  }
}
