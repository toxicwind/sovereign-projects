# === secrets & local env ===
.env
.env.local
.envrc.local
.secrets
*.key
*.pem

# === runtime / state ===
.state/
.prometheus/
logs/
*.log
pids/

# === python / node / rust builds ===
.venv/
__pycache__/
*.pyc
*.egg-info/
node_modules/
target/
dist/
build/

# === models & heavy data - GGUFs live in ~/projects/models ===
models/
*.gguf
data/

# === scratch ===
scratch/
bins/
test.zsh

# === IDE ===
.idea/
.vscode/

# === direnv / nix - retired but keep dir to ignore ===
.direnv/
.devenv/
.devbox/

# === llama-swap - keep configs, ignore only binaries and db ===
tools/llama-swap/llama-swap
tools/llama-swap/ollama-proxy
tools/llama-swap/*.db
tools/llama-swap/*.db-journal