# === secrets & local env ===
.env
.env.local
.envrc.local
.secrets
*.key
*.pem

# === runtime / state ===
.state/
.prometheus/
logs/
*.log
pids/

# === python / node / rust builds ===
.venv/
__pycache__/
*.pyc
*.egg-info/
node_modules/
target/
dist/
build/

# === models & heavy data (GGUFs live outside this tree) ===
models/
*.gguf
data/

# === scratch, agent ephemeral, local overrides ===
scratch/
bins/
test.zsh
.agents/
.grok/
*.local.md
*.local.toml

# === backups & reorg dumps (never stage) ===
backup/
**/reorg_*/
**/cleanup_*/
hw_dump*
hardware_audit*

# === non-production trees (live under backup/ if needed) ===
yote/
tools/legacy/
tools/ollama-proxy-rs/

# === rust_algo_web: track source, never target or fat binaries ===
rust_algo_web/target/
bin/sovereign_web
bin/sovereign_devops_advisor

# === IDE ===
.idea/
.vscode/

# === retired env managers ===
.direnv/
.devenv/
.devbox/

# === llama-swap: keep config.yaml + docs only ===
tools/llama-swap/llama-swap
tools/llama-swap/ollama-proxy
tools/llama-swap/*.db
tools/llama-swap/*.db-journal
tools/llama-swap/config.20*.yml
tools/llama-swap/config.pre.yaml

# === random experiment / one-off noise ===
avx512_test*
*.bt
t.txt
patch.sh
builds-dynamic.json
tree.txt
mise.toml.txt
rust_algo_web.zip.pdf
dashboard/
llama-swap-maximal*/
ports.env
