{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "title": "Fleet-Bench GPU Power",
  "uid": "fleet-power",
  "panels": [
    {
      "title": "GPU Power Draw",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        { "expr": "fleet_gpu_power_draw_watts", "legendFormat": "W" }
      ],
      "fieldConfig": { "defaults": { "unit": "watt" } }
    },
    {
      "title": "GPU Temperature",
      "type": "gauge",
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 0 },
      "targets": [
        { "expr": "fleet_gpu_temperature_celsius", "legendFormat": "°C" }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "celsius",
          "min": 0,
          "max": 95,
          "thresholds": {
            "steps": [
              { "color": "green", "value": null },
              { "color": "yellow", "value": 75 },
              { "color": "red", "value": 88 }
            ]
          }
        }
      }
    },
    {
      "title": "Current Power",
      "type": "stat",
      "gridPos": { "h": 4, "w": 6, "x": 18, "y": 0 },
      "targets": [
        { "expr": "fleet_gpu_power_draw_watts", "legendFormat": "W" }
      ],
      "fieldConfig": { "defaults": { "unit": "watt" } }
    },
    {
      "title": "GPU Utilization",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "targets": [
        { "expr": "fleet_gpu_utilization_percent", "legendFormat": "%" }
      ],
      "fieldConfig": { "defaults": { "unit": "percent" } }
    },
    {
      "title": "GPU Memory Used",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "targets": [
        { "expr": "fleet_gpu_memory_used_mb", "legendFormat": "MB used" }
      ],
      "fieldConfig": { "defaults": { "unit": "megabytes" } }
    },
    {
      "title": "Fan Speed",
      "type": "stat",
      "gridPos": { "h": 4, "w": 12, "x": 0, "y": 16 },
      "targets": [
        { "expr": "fleet_gpu_fan_speed_percent", "legendFormat": "%" }
      ],
      "fieldConfig": { "defaults": { "unit": "percent" } }
    }
  ],
  "schemaVersion": 39,
  "tags": ["fleet-bench", "gpu"],
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "version": 1
}
