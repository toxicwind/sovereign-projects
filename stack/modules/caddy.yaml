processes:
  caddy:
    command: caddy run --config ./Caddyfile
    working_dir: .
    availability:
      restart: on_failure
      backoff_seconds: 3
      max_restarts: 10
    log_location: .state/logs/caddy.log
