# MEMORY.md

<!-- Your curated long-term memory: durable facts, preferences, and commitments. Keep it tight: promote what lasts here, and leave raw day-to-day detail in your daily notes. -->

## Facts

## Preferences

## Commitments

## Paper router legs (2026-09-14)
- `papers.py` now has 6 free search legs: arXiv, OpenAlex, Semantic Scholar, DBLP, Hugging Face Papers, **alphaXiv** (community index: upvotes, topics, linked GitHub repos).
- **OpenCitations** wired as citation-graph enrichment in `--id` mode for DOI inputs (no key, 180 req/min/IP): incoming citations + outgoing references.
- `--id` accepts arXiv ID or DOI; alphaXiv also attaches up to 8 similar papers per ID lookup. `--id` output capped to 1 paper, exact-ID matches preferred over search neighbors.
- **JSONL everywhere**: `--format jsonl` emits meta line + one `{"type":"paper"}` per line; pipes into jq/sqlite/pandas.
- **`--s2dupe`** (with `--id`): free dupe of Semantic Scholar's key-gated features — tldr (S2's own free tldr field when served, else extractive local), **influential citations** (2-hop: OpenCitations citing DOIs → one batched OpenAlex `filter=doi:a|b` for citer cited_by_count, ranked), **citation velocity** (cites/year from OC creation dates). Citation parts need a DOI; TLDR runs on any abstract. New route.py fns: `tldr_extractive`, `influential_citations`, `citation_velocity`, `s2_free_lookup`, `openalex_lookup_doi` (exact `works/doi:` lookup).
- OpenAlex quirk found 2026-09-14: batch OR filter must be single-field `doi:10.1|10.2` — repeating the field (`doi:10.1|doi:10.2`) returns HTTP 400.
- **Audit system** (2026-09-14): every `papers.py` run appends one JSONL line to `~/.cache/shingle/papers_audit.jsonl`; `papers.py --audit [--audit-window 1h|24h|7d|all]` reports per-leg ok-rate, p50/p95/max latency, error taxonomy (http_429/timeout/bot_check/connection/other), fail-fast score, shared arXiv attempt log, generated recommendations. `--no-audit` skips logging; `--audit-log PATH` overrides.
- **Completions** (2026-09-14): `bin/completions/papers.bash` + `papers.zsh` (all flags; value completion for --sort/--format/--audit-window).
- **Repo comparison** (2026-09-14, via new `github/bin/repo_search.py`, best-match order): closest analogs are `alexfdez1010/paperhound` (7 sources incl. CORE/Crossref, PDF download+docling→markdown, local library, refs/cited-by commands) and `hossam1522/VerifiSci` (Go CLI for LLM agents); `danielnsilva/semanticscholar` is the de-facto S2 client (typed, async, exposes real tldr/influentialCitationCount — needs S2, key optional). Our edges: no-key s2dupe, per-leg rate-limit audit, JSONL-first, fail-fast redundancy, alphaXiv community signal. Gaps vs paperhound: no PDF download/convert, no local library, no rerank.
- alphaXiv endpoints used: `GET /v1/search/paper`, `GET /papers/v3/legacy/{arxiv_id}`, `GET /papers/v3/{arxiv_id}/similar-papers` (takes arXiv ID, not UUID). Search/metadata/similar are public; key only unlocks library/private features.
- User created an alphaXiv API key on 2026-09-14; connector `custom.alphaxiv` (api.alphaxiv.org, Bearer header) set up but key NOT yet stored in vault — capture link sent, pending user action. Key must never be written to files/memory; router degrades gracefully to public endpoints until stored.

## NIM completions (2026-09-14)
- **`nvidia-nim-loader`** skill: runs LLM completions through NVIDIA's hosted NIM API (`https://integrate.api.nvidia.com/v1`, OpenAI-compatible, 130+ models). External isolated venv at `~/workspace/skills/nvidia-nim-loader/.venv` (openai lib installed); `bin/nim.py` = `models` (list ids) | `chat --model ID` (prompt or stdin, `--system/--temperature/--max-tokens/--format json|text`) | `ping`. Auth via Secure Vault `custom.nvidia` (Bearer `nvapi-…`) through the dynamic credential surrogate — raw key never readable/printed/stored; graceful `no_nvidia_credential` error when missing.
- **`model-ranking`** skill: `bin/rank.py --task tldr|rerank|chat|reasoning|agent|longctx|fast --top N --format table|json|id` ranks a curated Sept-2026 NIM model table (heuristic quality/speed/context/tools scores). `--format id` feeds the loader: `nim.py chat --model $(rank.py --task tldr --format id)`.
- **`papers.py --tldr-llm`** (with `--id`): replaces extractive TLDR with abstractive NIM TLDR (model from ranking skill, `--nim-model` overrides). Falls back to extractive on any failure; own `tldr-llm` leg entry. Verified 2026-09-14 on 2302.01318: degraded cleanly with `no_nvidia_credential`, extractive TLDR intact.
- **nvapikey status 2026-09-14**: key stored as `custom.nvidia` and VERIFIED working (`nim.py ping` ok). Live /v1/models catalog pulled same day (82 models); ranking table rebuilt with `avail` field (verified/listed/flaky/eol) and availability-weighted scoring. `nvidia/nemotron-3-super-120b-a12b` is the verified default (tldr leg ok, ~3.3s). Catalog traps found: `nvidia/nemotron-3-nano-30b-a3b` = HTTP 410 end-of-life; several listed ids 404 for this account/key; big models cold-start slowly (timeouts at 40-60s).
- **Complete 82-model audit 2026-09-14** (pushed to repo, README rewritten): fail-fast ladder (5s ceiling, no retries), verdicts — 55 dead-404-gated, 10 alive-fast, 9 timeout-fast, 3 unavailable-503, 3 error-other, 2 streaming-noheaders. Category x verdict: LEGACY 21/21 gated, EMBEDDING 7/7 gated, REWARD 1/1 gated, GUARD 5/5 respond somehow (most callable category), CHAT 32 only 5 alive, NEMOTRON-3 5 with 1 alive (ultra-550b) + 2 on 503 (super-120b, omni). Of the 10 alive, only ~4 are general chat models (rest: 3 guards, 2 riva-translate, vision, diffusion, parse). Alive list: diffusiongemma-26b, llama-3.2-11b-vision, safety-guard-8b-v3, ultra-550b, nemotron-3.5-content-safety, nemotron-parse-2.0, riva-translate v1.1+v2, gpt-oss-20b, glm-5.3-flash.
- **NIM availability is nondeterministic + catalog mutates**: across runs minutes apart ultra-550b dead->alive, super-120b alive->503, deepseek-v4-pro alive->timeout->DELISTED from /v1/models (82->81 models within ~10 min). EOL models are delisted, not 410'd (nano-30b gone). GET /v1/models/{id} = 200 for everything listed = existence oracle only, not entitlement. Fuzz (25 probes): only real inference route is POST /v1/chat/completions; /v1/embeddings is model-aware (nv-embed-v1 -> 410 EOL 2026-08-25); n=2+temp0 -> 400; nemotron-parse v1 400s on plain strings. super-120b serves `deprecation: 2026-10-03T09:00:00Z` and 503'd — default model is dying.
- **api-fuzzing skill installed** 2026-09-14: ~/workspace/skills/api-fuzzing/ (from langbyyi/CyberStrikeAI-SRC, pushed 2026-09-13; beat ffuf-web-fuzzing = path discovery only, and low-level-dev-skills = libFuzzer/C binaries wrong domain).
- **Public repo 2026-09-14**: `toxicwind/nvidia-nim-model-probe` (https://github.com/toxicwind/nvidia-nim-model-probe) — fail-fast probe of 19 NIM models (only 5 usable: super-120b, deepseek-v4-pro, glm-5.3-flash, gpt-oss-20b alive-fast; gemma-4 cold-start; deepseek-v4-flash streaming-stall; ultra-550b/lightning/kimi-k3 flaky-timeout; omni-reasoning 503; 8x 404-gated; nano-30b 410-EOL). Contains probe.py/bench.py/nim.py/rank.py (public variants w/ auth.py shim, NVIDIA_API_KEY fallback), sanitized data/ (account IDs redacted), star-bait README w/ per-model deep dives + deep links. Update README when Exa/GitHub research lands.

## Exa usage audit (2026-09-14)
- Exa has NO balance/usage endpoint on api.exa.ai (`/usage /billing /account /credits` all 404, verified live; web research confirms). Only official usage API is `admin-api.exa.ai/team-management/api-keys/{id}/usage` — needs a separate service key, returns spend not remaining. Balance lives only in dashboard.exa.ai.
- Every `/search` response carries `costDollars.total` (real billed estimate, e.g. $0.007/search) — the audit now captures it per call instead of estimating.
- Local ledger: `~/workspace/skills/emergent-enrich/bin/exa_audit.py` -> `~/.cache/shingle/exa_audit.jsonl`; `route.py --audit [--audit-window]` summarizes. Session spend so far: 5 calls, 41 results, ~$0.21.
- Credit exhaustion = HTTP 402.
- **Team-management API verdict 2026-09-14**: `admin-api.exa.ai` is REAL (garbage path -> 404 `{"error":"Not Found"}`, real path -> 401 `{"error":"Unauthorized"}` — routing works, auth enforced). But the "service key" it demands ("Service API key for team authentication" per Exa's own OpenAPI spec) is a separate credential type the user's dashboard does not offer — likely team/enterprise-only or otherwise ungeneratable on their account. `custom.exa-service` connector was created but its stored key 401s; endpoint unreachable for now. Even if reachable, it returns *spend* (`total_cost_usd`), never remaining balance.\n- **`/v0/teams/me` 2026-09-14**: documented "Get Team Info" endpoint on api.exa.ai (regular key, returns concurrency/limits) — live-probed, HTTP 404 `tag:NOT_FOUND`. Not available on this account.

## Skills workspace (2026-09-14)
- 40 skills installed from Drive bundle into ~/workspace/skills/, plus new `skill-setup` harness (41 total).
- Shared portable venv: ~/workspace/skills/.venv (Pillow, numpy, python-dotenv, requests, pandas, networkx, pydantic, nest_asyncio, curl_cffi, camoufox); requirements.txt alongside. runner.sh files prefer it on PATH.
- Port-holding skills fixed to read ports from loaded env: direct-socket-task-runner (CDP_PORT, was hardcoded 9223), kimi-container-runtime (KIMI_GATEWAY_PORT), sovereign-egress-orchestrator (EGRESS_PORT), pitchfork-service-triage (MCP_PROXY_PORT), asymmetric-procedural-orchestrator (PROXY_EGRESS_URL).
- `skill-setup/main.py`: --list/--check/--setup/--smoke/--report/--ports/--dns. --ports discovers all skill ports (configured from .env, documented in SKILL.md), listening state via /proc, owning PID/process; run wrapped: `unshare -U -r python3 main.py --ports`.
- Configured ports: 5901/6080/9223 (vnc/cdp), 8080 (asymmetric-procedural-orchestrator PROXY_EGRESS_URL), 25109 (pitchfork MCP proxy), 25126 (kimi gateway), 25127 (sovereign egress).
- cdp-namespace-controller + web-tool-vnc-wrapper runners: read .env, audit procs/sockets, unshare check, probe TRUSTED_HOSTS (default 127.0.0.1), chromium --remote-allow-origins built from trusted hosts.
- /home/toxic refs corrected to /home/hatch everywhere except env-log-debugger (purposefully external: describes awrawr-pc target machine, marked as such).
- EXA_API_KEY (same key) in 7 skills, all return HTTP 200. Only credential-like var across all .env files.

## Trusted DNS fix (2026-09-14, permanent)
- Local resolver is an unreliable narrator: sinkholes domains to 198.18.0.0/15 (seen for annas-archive.org/.se, intermittently example.com) instead of real answers; DoH (1.1.1.1, dns.google) gives truth.
- Fix: ~/workspace/skills/shared/trusted_dns.py — doh_resolve/resolve_host/patch_socket/pinned_session (curl_cffi CURLOPT_RESOLVE pinning, SNI intact)/fetch with backoff. Patch covers getaddrinfo AND gethostbyname/gethostbyname_ex (C gethostbyname bypasses getaddrinfo-only patch). System DNS fast-path kept unless all answers sinkholed; definitive DoH NXDOMAIN raises instead of returning the lie.
- Permanently attached: venv site-packages/zzz_trusted_dns.pth auto-runs trusted_dns_bootstrap at every venv interpreter startup (venv sitecustomize.py does NOT work — /usr/lib/python3.12/sitecustomize.py takes precedence). Canonical copies in skill-setup/assets/; `--setup` redeploys drifted files; `--dns` ensures + live-canary tests.
- Verified 2026-09-14: example.com -> 104.20.23.154 (was sinkholed), annas-archive.se -> gaierror, annas-archive.is fetch 200 via DoH-pinned curl_cffi, pip unaffected.

## Encrypted GitHub FS (2026-09-14)
- Public repo `toxicwind/vaultfs` (https://github.com/toxicwind/vaultfs) — holds ONLY ciphertext: random-name encrypted blobs + encrypted manifest + README explaining it's ciphertext + .gitattributes routing blobs/** to LFS. Public viewers see nonsense.
- Skill: ~/workspace/skills/encrypted-github-fs/ (SKILL.md, main.py, runner.sh, README.md). CLI: init | put <src> [-a alias] | get <alias> [-o out] | list | rm <alias> | sync [--pull].
- Crypto: Fernet key in skill `.env` as VAULTFS_KEY (name only — never the value; never committed/uploaded/printed). Blobs = Fernet(zlib-9(data)); manifest = Fernet(zlib-9(json)). put dedupes by SHA-256. Key loss = vault loss, no recovery.
- Sync via GitHub Contents API (api.github.com, custom.github surrogate); no git push. Prunes remote orphan blobs on sync.
- git-lfs 3.6.1 binary at ~/workspace/bin/git-lfs (ephemeral /usr avoided). `cryptography` added to skills requirements.txt + venv.
- Tested 2026-09-14: put/get roundtrip OK, no plaintext in blobs/manifest, pull-from-scratch OK, remote pruning OK, skill-setup --check PASS.
- **Dual-routed 2026-09-14**: skill now routes by type — documents (pdf/epub/mobi/txt/docx/...) go plaintext to Google Drive folder "vaultfs" (live API via hatch_gws_cli, local store/drive.json alias→fileId index, rm = trash); everything else goes to the encrypted vault. `--to vault|drive|auto` overrides. Drive folder auto-created on first drive put; verified put/get byte-identical/rm/list end to end.

## Portable binaries ZipFS (2026-09-14)
- Skill: ~/workspace/skills/portable-binfs/. Bundle: assets/portable-binaries.zip (bin/<name> + manifest.json {version, sha256, size, source, added_at}), staging area ~/workspace/bin/.
- CLI: build [--src] | list | verify | mount [--target] | exec <name> [args] | which <name> | path. Extraction to skill-local .local/bin, exec bits preserved, re-extract on manifest drift.
- Currently bundled: git-lfs 3.6.1 (12MB). Workflow: drop binary in ~/workspace/bin → `main.py build` → verify.
- Code portability audit 2026-09-14: zero hardcoded /home paths in any skill .py/.sh (skill-setup computes SKILLS_DIR/VENV_DIR from __file__; annas-router resolves shared/ relative to its own dir; trusted_dns path-free; .pth content generated dynamically). Remaining /home/hatch refs are bundle .env values (real user config dirs) and env-log-debugger's intentionally-external docs.

## Anna's Archive downloads (2026-09-14)
- Current site version gates ALL downloads (fast + slow partner servers) behind account login — every button hard-links to /account in server HTML, confirmed in live Chromium AND full-stealth Camoufox. Legacy no-login routes (/slow_download/<md5>/0/0, /md5/<md5>, /dyn/downloads/check_downloaded/) all 404 on this version. Page copy still claims "no membership requirement" on slow side, but no no-login route exists.
- annas-router SKILL.md updated with this; CLI stays search/metadata-only. Downloads need the user signed in via live browser.
- Law of One books also freely available: Internet Archive "All Law of One Books" (PDF/EPUB/txt, no login) and L/L Research's own free PDFs (assets.llresearch.org).
- **Missing Wilcock books** (The Source Field Investigations, The Synchronicity Key): IA has them restricted/borrow-only. Next route = LibGen direct search (probing mirrors 2026-09-14).
- **User-provided batch 2026-09-14** (user gave up on the hunt and uploaded files): `~/workspace/wilcock/user/process_local.py` validates/converts/uploads local files. Done: Synchronicity Key EPUB (40 chapters, 170,267 words), Reincarnation of Edgar Cayce PDF (410 pp, 113,165 w), wilcock-resignation.pdf (10 pp, 4,355 w), Cosmic Disclosure S1-12 PDF (3,620 pp, 1,019,929 w). Originals -> Drive (aliases `wilcock-user/*`), parquets -> encrypted vault. **EPUB bug fixed**: book-to-dataframe used wrong OPF namespace `http://www.opf.org/2007/opf`; now tries correct `http://www.idpf.org/2007/opf` first.
- **Still missing**: The Source Field Investigations (no file obtained).
- **Source Field Investigations FOUND 2026-09-14** via emergent-enrich router (GitHub code search -> Exa, credits spent: true). GitHub gave only an affiliate metadata repo (techidaily/site-novels-ebooks, no files); Exa surfaced open IA item `sourcefieldinvestigations` (NOT the restricted `isbn_9780452297975`) with direct PDF+EPUB. Downloaded 6.1MB PDF, processed via process_local.py: 563 pages, 175,276 words, Drive + vault. All 5 Wilcock user-files now complete (4,643 rows, 1,482,992 words total).

## Playwright headless in sandbox (2026-09-14)
- playwright 1.62.0 installed in skills venv. Raw Chromium could NOT egress at first (ERR_EMPTY_RESPONSE): it can't speak IPv6 to the sandbox egress proxy (hatch-egress-proxy resolves IPv6-only) and its background networking (component update, google pings) breaks navigation.
- Fix: `annas-router/bin/proxy_fwd.py` bridges 127.0.0.1:3129 -> proxy IPv6:3128 (proxy allows unauth CONNECT from this VM, verified). Launch Chromium headless with --proxy-server=http://127.0.0.1:3129 --disable-component-update --disable-background-networking --ignore-certificate-errors (egress proxy MITMs TLS). Keep proxy_fwd.py running in background for headless work. `bin/probe_annas.py` = working headless probe of Anna's download buttons.
- GitHub recency-ranked Anna's tools (2026-09-14): zelestcarlyone/stacks (816*, upd 2026-09-13, needs membership for fast dl), ALBEDO-TABAI/annas-archive-downloader "aget" (Playwright, upd 2026-08-16, slow-dl then LibGen fallback, member key for fast), proItheus/AA-add-dllink (userscript adding libgen links). All assume membership or working slow downloads; none bypass the current account wall.
- annas-archive.li redirects headless browsers to external bot-check (usokac.com) — dead end from here.

## Model-card audit (2026-09-14)
- Every build.nvidia.com model page has `<link rel="alternate" type="text/markdown" href=".../{org}/{model}.md">` — 51/81 live catalog models have fetchable cards (no browser needed); 30 have no public card (404), incl. riva-translate-v1.1, several guards, legacy models. Website card coverage is a SUBSET of the API catalog.
- Cards prove the /v1/models catalog is modality-blind: nemotron-3-embed-1b card output = "Floats", nvclip = "Float tensor", omni-reasoning = Video/Audio/Image/Text in — all listed in the chat catalog, all 404-gated for chat POSTs. The API never declares which ids are chat-capable; only probing does.
- **Ising "physics model" explained**: nvidia/ising-calibration-1.5-31b card = dense multimodal VLM built on Gemma 4 31B for quantum calibration plot analysis, Text+Image in, structured text out. Weird name, normal chat endpoint. It timed out in probes (cold), not dead.
- **Deprecation signals live in API headers, not cards**: super-120b's card says nothing about retirement; the API serves `deprecation: 2026-10-03T09:00:00Z`. Cards are marketing-fresh, headers are ops-fresh.
- Headless Chromium now available: chrome-headless-shell 151 zip in ~/workspace/headless/ (see TOOLS.md); playwright's own CDN blocked by egress gateway, use storage.googleapis.com chrome-for-testing.
- api-fuzzing skill rewritten as v1.2-local: environment tool mapping, fixed dead Related Routing links, rate-limits-audited-not-bypassed, DNS/auth/nondeterminism lessons.
- **Browser-skill audit 2026-09-14**: 16 skills mention browsers; 10 updated with headless+egress reality (chrome-headless-shell-151.zip in ~/workspace/headless/, proxy_fwd.py 127.0.0.1:3129 bridge, --ignore-certificate-errors, trusted_dns.patch_socket()). Full launch recipe in cdp-namespace-controller/web-tool-vnc-wrapper/direct-socket-task-runner/resilient-agent-kernel; Camoufox skills (emergent-*-dissector x3, systemic-dissection-orchestrator) got proxy_uri default note; annas-router's stale /opt/meta-chromium/chrome path fixed. 4 skills with incidental mentions skipped.
- **GitHub Pages LIVE 2026-09-14**: https://toxicwind.github.io/nvidia-nim-model-probe/ (verified 200). 11 files in docs/ + data/. Token needed Pages write scope (user upgraded). Generator: ~/workspace/sitegen/gen.py.
- **toxicwind/nvidia-nim fork 2026-09-14**: forked api-evangelist/nvidia-nim (OpenAPI specs for NIM) into our org to patch (mislabeled BioNeMo titles, missing NVIDIA extensions like extra_body/chat_template_kwargs, doc-vs-reality gaps: 403 documented vs 404 observed). Spec kept intact in ~/workspace/skills/nvidia-nim-loader/ts-client/spec/.
- **tau repo state 2026-09-14** (toxicwind/tau, formerly OMP/oh-my-pi, Bun TS monorepo 6752 files): NVIDIA provider exists at packages/ai/src/registry/nvidia.ts (login via pasted nvapi key, validates against nvidia/llama-3.1-nemotron-70b-instruct — now 404-gated/dead). Transport = generic openai-completions. Quirks already handled: chat_template_kwargs.enable_thinking for Qwen, DeepSeek DSML token stripping, empty-completion retry (2x), reasoning-effort 400 fallback. Catalog default model = llama-3.1-nemotron-70b-instruct (stale). Gaps for our audit to patch: validation model dead, no force_nonempty_content for Nemotron-3, no cold-start model awareness (kimi-k3 43s, ultra 41s), no deprecation-header surfacing (super-120b EOL 2026-10-03).
- **Fork decision 2026-09-14**: user corrected — tau IS "our fork" (of oh-my-pi/OMP); the new toxicwind/nvidia-nim fork was my misread of "our fork". User chose to KEEP the nvidia-nim fork too (spec patch home: mislabeled titles, missing NVIDIA extensions, doc-vs-reality gaps).
- **Repo audit 2026-09-14 — which tau is canonical**: `toxicwind/sovereign-projects/tau/` is the better/newer tau (HEAD 2026-09-12 maximal-reconcile; README-FORK.md: engine/ = working fork of oh-my-pi, vendor/ = read-only upstream mirror). Provider config migrated from registry/nvidia.ts to KDL compat rules (providers/nvidia.kdl: thinking-mode "effort"; auth/nvidia.kdl). Already has nemotron-effort routing (engine/packages/ai/src/routing/nemotron-effort) + test-nemotron-effort.ts. Same rot: auth KDL still validates against dead llama-3.1-nemotron-70b-instruct. Standalone `toxicwind/tau` (2026-09-11) = older flat snapshot. `toxicwind/sovereign` = ops/workspace repo, not the engine. `toxicwind/oh-my-pi` = pre-rename, stale 09-03. Patch target = sovereign-projects/tau/engine.

## awrawr-mcp bridge (2026-09-14)
- Funnel routing: Tailscale serve STRIPS the `/mcp` mount prefix when proxying (backend saw `POST /` -> 404). Fixed via proxy target `http://127.0.0.1:8377/mcp` using idempotent `~/funnel_fix.py` (LocalAPI serve-config merge).
- HTTP 421 "Invalid Host header": MCP SDK DNS-rebinding protection — FastMCP defaults allowed_hosts to localhost only, funnel forwards public Host. Fixed with `TransportSecuritySettings(allowed_hosts=[localhost variants, funnel host, funnel host:*])`.
- Auth at transport layer: Starlette `HeaderTokenAuth` middleware checks `X-MCP-Token` vs `~/.awrawr_mcp_token` (401 otherwise). Tool is now `exec(cmd, workdir)` — no token arg (vault surrogate only guaranteed in header). Server builds app via `streamable_http_app()` + `add_middleware()` + uvicorn directly.
- Skill client `bin/exec.py` sends `{"cmd","workdir"}` only; surrogate placed in X-MCP-Token by `add_surrogate_to_request`.
- Server: `~/awrawr_mcp.py` on awrawr-pc, venv `~/.awrawr-mcp-venv`, token `~/.awrawr_mcp_token` (600), log `~/.awrawr_mcp.log`. Workspace copy: `~/workspace/your_files/awrawr_mcp.py`. `MCP_FUNNEL_HOST` env overrides allowlisted host.


- 2026-09-14 ~02:57 MDT: systemd user service `awrawr-mcp.service` enabled + active on awrawr-pc (`~/.config/systemd/user/`), `Restart=always`, linger enabled (survives logout/reboot). Bridge re-verified through it: `systemctl --user is-active` -> active.
- GitHub research delegated to subagent: find mature upstream MCP remote-shell/SSH/Tailscale project to replace the hand-rolled bridge per user's "maximal first" requirement. Findings to be recorded here on return.
- **Repo consolidation COMPLETE 2026-09-14**: user confirmed deletion; `toxicwind/tau` and `toxicwind/oh-my-pi` deleted via API (both 204, verified 404 after). All 619 orphan files preserved in sovereign-projects `tau/archive/` (commit ee53e9ce). `sovereign-pi` kept (28 branches + 20 tags).

- **GitHub research COMPLETE 2026-09-14** (subagent, verified via GitHub API + READMEs): no mature drop-in streamable-HTTP remote-exec MCP server exists. Findings:
  - `gelse/ssh-mcp` (7*, MIT, Python, pushed 2026-09-14, ~6 wks old): ONLY candidate matching our shape — streamable HTTP at /mcp, header auth (X-API-Key/Bearer, PBKDF2-hashed), 9-layer authz chain, per-key command policies/allowlists, shell-injection detection, rate limiting, JSONL audit logs, SFTP. SSH-gateway arch (would SSH to 127.0.0.1:22 on awrawr-pc). Track as migration candidate; revisit in 1-2 months.
  - `tufantunc/ssh-mcp` (726*, MIT, TS, pushed 2026-09-14): community leader, best security model (policy matrix, HITL approvals, quotas, host-key pinning, audit) — but stdio SSH *client* meant to run agent-side; sandbox isn't on tailnet, so blocked for our funnel path.
  - Also-rans: `sonirico/mcp-shell` (106*, Go, stdio-only), `Servagent/servagent` (dead 7 months), `chouzz/remoteShell-mcp` (stdio-only), `frah/ssh-mcp` (stale), `carstenf/hetzner-mcp-server` (0*, same class as ours). No Tailscale+MCP product exists; our funnel pattern is already community-standard. No installable skills found for any of these.
  - Recommendation: harden current bridge in place (command allowlist/denylist, JSONL audit log — ~60 lines) rather than betting on 7-star project. Revisit gelse/ssh-mcp in 1-2 months.
- **sovereign-pi merged 2026-09-14**: main-branch orphans recomputed vs current tree (earlier archives covered 264) -> 96 files archived to sovereign-projects `tau/archive/from-sovereign-pi/` (commit b032e467; README updated 230c9ef1). `.pi/sessions/*.jsonl` (3) EXCLUDED — private session logs, sov-proj is public. 28 branches = stale upstream oh-my-pi PR branches (v0.0.3-era lineage, not Chris's work); 30 tags = upstream releases. Repo RENAMED (not deleted) to `toxicwind/sovereign-pi-archive` (private) preserving branches/tags/sessions/history. User may still delete it.

- **YOLO mode added 2026-09-14** (user asked: "it should have yolo lol"): `#yolo ` command prefix bypasses the denylist — still header-authenticated, still audited (flagged `yolo:true`). Deployed through the bridge itself via base64'd patch script (`~/workspace/your_files/yolo_patch.py`, fail-fast string replacement). Verified live: `echo reboot` -> POLICY DENIED, `#yolo echo reboot` -> prints "reboot", audit log shows `"yolo": true`. Workspace copy + SKILL.md synced.
- Deploy lesson 2026-09-14: NEVER restart `awrawr-mcp.service` from inside a bridge call — the restart SIGTERMs the calling shell (exit=-15, response lost). Patch/compile first, restart, then verify with a FRESH bridge call. To deploy file changes through the bridge, base64 the payload (`echo '<b64>' | base64 -d > /tmp/f`) to dodge heredoc/quoting layers; invoke skill via subprocess argv (no shell).

- **Parquet audit live 2026-09-14**: `~/awrawr_mcp_audit_export.py` on awrawr-pc compacts `~/.awrawr_mcp_audit.jsonl` -> `~/.awrawr_mcp_audit.parquet` (pyarrow 25.0.1 installed in bridge venv, snappy, typed schema: ts timestamp[us,tz=UTC], cmd/workdir/status/reason strings, yolo/truncated bools, exit/out_chars/elapsed_ms int64; garbage lines + bad timestamps skipped). Daily systemd timer `awrawr-mcp-audit-export.timer` enabled (Persistent). First run: 9 rows, 1 denied, 1 yolo. JSONL stays the append-safe source of truth. Convention matches book-to-dataframe skill (pyarrow+snappy).
- **Sovereign audit 2026-09-14** (`/home/toxic/sovereign`, 60G; disk 61% used, healthy): tau/ 34G (engine/target 16G = Rust build artifacts, engine/node_modules 1.5G, vendors 1.9G), projects/tau/ 18G = REAL dir not symlink (possible duplicate of tau work — flagged to user), rust_algo_web 3G, tools 983M, top-level node_modules 598M, herd 504M. Junk: file literally named as a python one-liner (131B), empty `--help`/`--version`/`uhh` dirs. Secrets hygiene: `.env`, `.env.local`, `.envrc` present (contents NOT read); `mock_token_file.txt`, `test_secret_sample.txt` at top level. audit/ has agents_md_inventory.json etc.

## nvidia-swarm-lens sanitize (2026-09-14)
- History purged of committed `.env` (held GitHub PAT + other API keys): rebuilt all 60 main commits via git API, deleting `.env` from the 15 trees that contained it. New main head `508fd0c4`; `maximal-merge` rebased onto it as `8724ce14`. Verified: zero commits touch `.env`, no `.env` in any tree on either branch.
- `.gitignore` already listed `.env` (it had been force-added).
- BLOCKED on going public until Chris revokes the exposed GitHub PAT and rotates the other API keys from that `.env` — committed secrets must be treated as compromised even in a private repo. Flip to public (`PATCH private:false`) only after his confirmation.

## tau-extensions (2026-09-14)
- `toxicwind/tau-extensions` published public (main, a52350a) from local `sovereign/tau-extensions/` tree — the at-risk untracked project is now backed up.
- Rebrand done via side chat: marketplace.json omp-extensions→tau-extensions, owner→toxicwind (commit e68376a).
- CI green 2026-09-14: root cause was `.gitignore` listing `src/`/`__tests__/` as stale artifacts, so the publish shipped zero sources; side chat restored 74 .ts files (33ff90a), my agent restored the typecheck job (c0daf3fd) — install + typecheck pass on main.

## Pup trix API key (2026-09-14)
- Chris's rule: the "pup trix" API key is for **debug or talking to Yote as Chris ("me") only** — nothing else.
- Exact service behind Chris's "pup trix" name TBD — Yote agent is identifying it in Yote's config during the Yote repair. Update this entry with the real service name once confirmed.

## awrawr-pc hardware audit v2 (2026-09-14)
- Rewrote audit: `~/workspace/skills/env-log-debugger/bin/hw_audit.sh` (portable) -> `/home/toxic/hw-audit.sh`. Root causes of v1: bare `jq -Rs` pretty-printed records (295 lines/147 records), NVIDIA parse split GPU names on spaces (now Python csv), hardware was base64-hidden not missing.
- v2: `/tmp/hw-audit-v2.jsonl`, 205/205 valid JSONL, line-1 truncation confirmed as paste artifact (v2 line 1 intact).
- Inventory: MSI PRO B650-VC WIFI (MS-7D78), BIOS AMI 1.L5 10/22/2025; 64GiB = 2x32GiB DDR5-6000 (UD5-6000, mfr Unknown, SNs 01046DE8/01046DF1) in A2/B2; 1x RTX 3090 24GB driver 610.43.03; disks: Samsung 870 QVO 1TB (46417h/27C), Seagate ST8000NT001 8TB (22995h/31C), WD SN850X 1TB (15556h/29C), Crucial CT1000E100SSD8 1TB (4980h/34C) - all SMART passed.
- Installed on awrawr-pc: msr-tools, nvme-cli, kcpuid (note: /usr/bin/cpuid also exists; both cpuid branches fired = 13 leaf records).
- vapoursynth.sh (50ms) disabled + pacman hook re-disables it; bash -lc 61ms -> 12ms.
- Nightly production: hw-audit.timer 03:17 (+-10min) -> /var/lib/hw-audit/audit-YYYYMMDD.jsonl, 30-day retention, enabled+active. Known intentional: mitigations=off, zram over zswap. Pending user review: 4 pacman orphans (fltk flxmlrpc hamradio-menus kcat-docs), unowned-alias classification.
- 2026-09-14: 4 pacman orphans identified — all from a scripted `pacman -S --noconfirm kcat` on 2026-09-03 (Kachina 505DSP ham-radio transceiver control, NOT Kafka kcat). fltk/flxmlrpc/hamradio-menus were its deps, kcat-docs from AUR/paru. Main kcat pkg later removed; nothing requires them now. Safe to `pacman -Rns` all four.

## Blanket permission (2026-09-14)
- Chris gave standing bruteforce permission: production-grade, no rollbacks, maximal, emergent, cutting-edge, nightly — all of the above, for me AND other agents. Relayed to main chat. Forward-only momentum; don't wait for taps.

## tau-extensions production pass (2026-09-14)
- Fix batch pushed: `c7be98b` (URL rebrand toxicwind/omp-extensions→tau-extensions everywhere, bun.lock name→@toxicwind/tau-extensions, ~/.omp→~/.tau in docs, omp-model-router moved to packages/omp-model-router + wired into workspaces/catalog, both marketplace.json manifests synced byte-identical at v0.2.0 with 3 plugins, README now lists 3 extensions), `c9e1bec` (AGENTS.md rewritten as clean public contributor guide — old one leaked /home/toxic paths, port numbers, mcpproxy config; NOTICE added with fork provenance: RekunDzmitry MIT, cakriwut/Riwut Libinuko MIT), `5f65766` (fixed AGENTS.md code spans).
- marketplace.schema.json URL returns HTTP 301 (exists, redirects).
- kimi decision: IMPLEMENT for real as packages/omp-kimi (kimi-code CLI integration per KIMI_CODE_FORK_RESEARCH.md — MoonshotAI/kimi-code, MIT, local binary /home/toxic/.kimi-code/bin/kimi). Builder subagent dispatched.
- Fork intake coordinator dispatched: 6 Tier-1 (pi-tasks, pi-workflow, pi-agent-browser-native, omp-best-of, gsd-omp, omp-undo-redo) + engram license check (thebtf/engram; vendor local copy if permissive). semantouch skipped (macOS-only).
- Standing agent rule (2026-09-14): agents EXECUTE while auditing — no read-only report-then-wait loop. Chris trusts them to fix as they go. Hard limits that still stand: secrets never in repos, don't break login shells (verify `bash -lc true`), don't delete user data.

- Ops lesson (2026-09-14): a daemon restart killed the omp-kimi builder mid-task and its uncommitted /tmp staging dir was lost. Workers doing repo work must commit early and often locally so restarts can't wipe progress.
- Org-wide repo audit COMPLETE (2026-09-14): ~/workspace/repo_audit_20260914.md — 451 repos (278 public/173 private/222 forks). SECURITY BLOCKERS for Chris: .env files in PUBLIC effusion-labs (mcp-stack/profiles/dev.env, prod.env) and PUBLIC sovereign (config/ports.env, skills/repo-audit/projects.env) — rotate creds + purge history; kimi_tokens.json copies in public sovereign-projects — verify not live. Worker-actionable: Tectonic Drift Detection CI failing 5/5 in 9 repos (fix at shared-workflow source), 93 stale public forks deletable, moonbox-live 705MB w/ 4 blobs >50MB, branch hoarding, 107 moonbox-* private repos backlog, missing licenses.

- **super-ralph audit 2026-09-14 (by Shingle directly)**: project at `/home/toxic/super-ralph` (moved from `/home/toxic/projects/super-ralph` — that is why it looked "missing"). Smithers-orchestrator ticket-driven autonomous dev workflow (scheduler/execution/merge-queue loops). Fork of `evmts/super-ralph` (origin is NOT Chris's — never push there) +3 local NIM-proxy commits (ff2e255, 15281f3, 8682388); dirty package.json is a trivial peerDep pin.
- **`claude` on awrawr-pc is a compiled bun shim** (`/home/toxic/claude-nim-shim.ts` → both `/home/toxic/.local/bin/claude` and `/usr/local/bin/claude`): every `claude` invocation goes to NVIDIA NIM chat completions. codex/gemini/kimi CLIs absent → agent fleet is 100% NIM-backed.
- **nim-proxy** (miztertea, Rust) RUNNING on 127.0.0.1:8000 since Sep 13, keyed mode. The shim BYPASSES it (direct to integrate.api.nvidia.com) — proxy pacing/multi-key not in the call path; needs an npk_ client key to wire up (do not hunt for it; ask Chris via vault).
- **Fixed 2026-09-14**: shim default model was `nvidia/nemotron-3-super-120b-a12b` (deprecated, EOL 2026-10-03, 503ing) → changed to `openai/gpt-oss-20b` (alive-fast), recompiled with bun, reinstalled both locations; backups in `/home/toxic/claude-bin-backup/`. `--version` verified; inference path untouched. No live ping possible from bridge (no NVIDIA_API_KEY in bridge env — key lives in Chris's session).
- ralph-test never completed (no SOVEREIGN_PROOF.txt); loop unproven end-to-end. Chris said do not run its workflows.


- **Local Git safety audit COMPLETE 2026-09-14 (coordinator a0907f6c, verified)**: all at-risk awrawr-pc work preserved in private `toxicwind/local-work-archive` — 471 working-tree repos manifest (md5-verified), 7 bare mirrors, stash byte-identical, sovereign +165 backed up (upstream push correctly skipped, remote moved), 21 dirty-worktree snapshot branches, artifact tarballs ≤45MB, tau-extensions preserved. Zero local modifications, no force-pushes, no deletions. Wave-2 remote deletions now safe to proceed.
- **NEW security finding**: 2 GitHub PATs in plaintext remote URLs in moonbox bare mirrors (`/home/toxic/projects/moonbox-live/.git_repos/`), plus `github_repos_pat.json` / `github_user_pat.json` in same dir (contents unread). Rotate these too.
- **Pending Chris decisions**: sovereign control-plane upstream fetch/rebase (local +165 vs remote 996931b2); confirm happy tau-extensions is public; dedup redundant scripts/skills copies (optional).

- **swarm-lens CI root cause 2026-09-14 (worker f7e175a6, final)**: `lint-and-test` is NOT broken — private-repo Actions $0 spending limit kills every job in 2–4s with 0 steps, no runner assigned (proven vs public-repo control case that ran green). Prior fix commits 646cbd3/50270bb1 chased a phantom; zero new commits pushed. Fixes: (1) self-hosted runner on awrawr-pc — runner v2.337.0 staged at ~/actions-runner-nsl/runner.tgz (inert, not configured), needs Chris's explicit go-ahead for registration-token handling + persistent infra; (2) Chris raises spending limit; (3) make public after key rotation (blocked). SYSTEMIC: same gate likely explains org audit's "Tectonic Drift failing 5/5 in 9 repos" — private-repo issue, not per-repo breakage. Relayed to CI-batch (2b24e61b) and Herd (b38843ae) workers: stop code-bruteforcing on 0-step instant failures, report the gate instead.

- **sovereign-router sanitation COMPLETE 2026-09-14 (worker 0a5d2dd2)**: both `toxicwind/sovereign-router` (11 commits rebuilt, d8aecdbd→96936b83) and `sovereign-router-backup` (single commit, 3295a07c→621a6fda) purged of `ultimate_extract/kimi_tokens.json` + `kimi_tokens_full.json` via Git API history rebuild; verified zero trees touch token paths, metadata parity 11/11; no force-pushes, sanctioned ref moves only. Both repos confirmed PRIVATE (exposure limited). ACTION FOR CHRIS: rotate the Kimi tokens in Kimi's dashboard — pre-sanitize commits linger as dangling objects on GitHub.

- **Tau NVIDIA provider hardening COMPLETE 2026-09-14 (worker 6a2585ad, verified by Shingle)**: origin=toxicwind/sovereign-projects, main==origin/main at 8b0d4c79, tree clean. Commits on top of canonical 8af98fdb: 40ff7acb (hygiene: removed stray kimi token files), 924f7c40 (nvidia-kdl: drop EOL-410 qwen3-next-80b-a3b-thinking, dedupe glm-5.3-flash), 8b0d4c79 (nvidia-ts: default gpt-oss-20b, drop stale nemotron-effort test, refresh provider-quirks, new nvidia-wire-compat test). Landed via sanctioned fast-forward, temp branch deleted, all 5 children done.
- Chris's call 2026-09-14: comfyui-offload (85G) STAYS on the 870 QVO. No move.
- Chris 2026-09-14: dnsmasq + kafka are SUPPOSED to be running (tau MCP infra) — their failed state is a real outage, not cruft. Maximal subagent dispatched to repair both + full ~/.tau audit-and-fix (config doesn't match real tau folders).
- Chris 2026-09-14: kafka + dnsmasq should likely be managed by sovereign's mise + pitchfork, not raw systemd units. Relayed to tau agent: fix/create pitchfork-mise definitions as the root-cause layer; don't leave systemd units and pitchfork fighting over the same ports.
- Chris 2026-09-14: no dual init systems — mise + pitchfork should own the majority of user toxic's services. Anomalous non-CachyOS-default systemd units get migrated to sovereign pitchfork. Second maximal subagent dispatched for the full unit-by-unit sweep (kafka/dnsmasq carved out, owned by tau agent).
- Chris 2026-09-14: maximal audit of pitchfork + mise layer itself ("it's a mess"). Third subagent dispatched: owns pitchfork.toml/mise.toml integrity (fix in place; additions left to systemd-migration sibling). Standing rule: never touch awrawr-mcp.service from inside a bridge call.
- nim-proxy deployment (verified 2026-09-14): Docker container `nim-proxy`, image `ghcr.io/miztertea/nim-proxy:latest`, 127.0.0.1:8000->8000/tcp, keyed mode. NOT owned by pitchfork (no definition) or systemd — manually run. Folded into the service-migration sweep: stray docker containers get pitchfork definitions too.
- Chris correction 2026-09-14: pitchfork.toml was NEVER meant to be hand-rolled — generator.ts (bun run scripts/generate.ts) is the intended source of truth. Prior "delete the generator" recommendation overridden: generator gets FIXED and reinstated. (c)+(d) plan amended: fix generator (missing port keys, drifted output), fold all current hand-edits back into generator sources, regenerate pitchfork.toml cleanly, then the rest of the plan (boot enable, supervisor restart, prune dead defs, upgrades as plain upgrades).
- Chris 2026-09-14: EXECUTE the amended (c)+(d) plan with MAXIMAL nightly mise feature use (latest docs). Execution agent dispatched, phased: Phase 1 (immediate — fix generator.ts code, upgrade mise+pitchfork, maximal stable feature adoption, serious [daemons]-vs-generator evaluation), Phase 2 (gated on tau agent's hand-edit ledger — fold into generator sources, regenerate pitchfork.toml, prune dead defs, clear pre-restart blockers, pitchfork boot enable, supervisor restart, verify).
- Chris 2026-09-14: mise-native WINS — generator.ts gets REPLACED, not fixed (supersedes the generator-as-source-of-truth correction). All service defs land in the mise-native layer; d8b4b3b5 owns that design. Migration worker b747b511 realigned 2026-09-14.
- Chris 2026-09-14: **mise native + direnv native** — BOTH stay, each native and coexisting (cancels the "kill direnv" workstream). b747b511's completed migration landed defs in generator sources; those get re-landed in the mise-native layer.
- Chris 2026-09-14: wants a fleet snapshot digest every 15 minutes (in addition to immediate completion/failure pings).
- **agent-chat-plugin code audit 2026-09-14** (`n24q02m/agent-chat-plugin`): README claims verified against source — agents mkdir their own channels, discovery is a directory scan, wait loop is pure sleep (zero model calls), no server/daemon/socket/subprocess anywhere; ~1,700 lines stdlib-only Python. Fork must-fix order: identity first (anyone can currently post as anyone, including as leader — needs per-agent message signing), then the 5s wait latency. Merge coordinator is building on this confirmed base. Runner-up `weijiafu14/agent-chatroom` rejected: no wait primitive, one-shot reads. This came from the fleet code-audit report in main chat when the user asked for emergent chat merge work, recorded 2026-09-14.
