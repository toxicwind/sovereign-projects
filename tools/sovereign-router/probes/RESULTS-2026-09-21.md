# Long-context engagement probe — results 2026-09-21

Per oracle verdict **DECISION 12097** (mission 4e621a18): the 1m-context promotion
is only decidable with passing probe data. This document is the probe data.

Probe: `long-context-probe.py` (same directory) — needle-in-haystack retrieval.
A synthetic haystack (~4 chars/token, seeded RNG, reproducible) with a unique
secret passphrase embedded at ~45% depth; the model must output ONLY the
passphrase. Accuracy = exact substring match. Sizes run 100k → 500k → 1M; a
context-cap failure stops the ladder (a lane capped below N cannot do N).

Session: `1m-probe-20260921-1745`. All runs recorded in the live health DB
`/home/toxic/sovereign/data/ast_matrix.db`, table `requests`,
`strategy='longctx-probe'`, `session_id='1m-probe-20260921-1745'`
(winner=1 iff HTTP 2xx AND accurate retrieval). Run JSONs: `runs-20260921/`.

## Evidence table

| Lane | Model | Size | HTTP | Outcome | Latency | Notes |
|------|-------|------|------|---------|---------|-------|
| nvidia (integrate.api.nvidia.com) | nvidia/nemotron-3-super-120b-a12b | ping | 503 | lane_down | 274 ms | "Service temporarily overloaded" |
| nvidia | nvidia/nemotron-3-nano-omni-30b-a3b-reasoning | ping | 503 | lane_down | 200 ms | "ResourceExhausted: Worker local total request limit reached (16/16)" |
| nvidia | nvidia/nemotron-3-super-120b-a12b | ping (retry +10m) | 503 | lane_down | 220 ms | still overloaded |
| nvidia | nvidia/nemotron-3-super-120b-a12b | ping (retry +25m) | 503 | lane_down | 227 ms | still overloaded |
| nvidia | nvidia/nemotron-3-super-120b-a12b | ping (retry +40m) | 200 | ok | 3381 ms | lane recovered |
| nvidia | nvidia/nemotron-3-nano-omni-30b-a3b-reasoning | ping (retry +40m) | 200 | ok | 1764 ms | lane recovered |
| nvidia | nvidia/nemotron-3-super-120b-a12b | **100k** | 200 | **ok** | 9179 ms | accurate retrieval, exact passphrase |
| nvidia | nvidia/nemotron-3-super-120b-a12b | **500k** | 200 | **ok** | 18214 ms | accurate retrieval, exact passphrase |
| nvidia | nvidia/nemotron-3-super-120b-a12b | **1M** | 200 | **ok** | 41426 ms | accurate retrieval, exact passphrase |
| openrouter | nvidia/nemotron-3-super-120b-a12b:free | ping | 200 | retrieval_fail | 361 ms | 200-EMPTY: valid HTTP 200, zero content bytes (known filtered-empty pattern) |
| openrouter | nvidia/nemotron-3-super-120b-a12b:free | ping ("What is 2+2?") | 200 | ok | 248 ms | real completion: "2+2 equals 4." — lane is alive; the earlier empty was prompt-specific |
| openrouter | nvidia/nemotron-3-super-120b-a12b:free | **100k** | 200 | **ok** | 1095 ms | accurate retrieval, exact passphrase |
| openrouter | nvidia/nemotron-3-super-120b-a12b:free | 500k | 400 | context_too_big | 186 ms | **provider cap: "maximum context length is 262144 tokens"** — hard ceiling, fail-fast |

## Findings

1. **A lane demonstrably sustains 1M context with accurate retrieval: the
   KEYED nvidia lane, `nvidia/nemotron-3-super-120b-a12b`** — 100k (9.2s),
   500k (18.2s), 1M (41.4s), exact passphrase every time. This is the first
   verified 1M-context evidence anywhere in the estate.
2. **The `:free` nemotron ID caps at 262,144 tokens** (stated by the endpoint
   itself) — 500k/1M impossible there. Its verified ceiling is 256k-class with
   accurate 100k retrieval in ~1.1s. A "free pool 1M default" cannot name the
   `:free` ID — any 1M promotion must name the KEYED lane explicitly.
3. **The nvidia lane flaps hard**: 503-down for ~40 min mid-probe (both IDs,
   ~200 ms fast-reject, "Service temporarily overloaded" / worker
   ResourceExhausted), then clean 200s. The router's elo already demotes it
   while flapping (904, half-open) — working as designed. Any promotion must
   not fight the circuit breaker.
4. **The 200-empty pattern is real but prompt-dependent**: a bare "reply PONG"
   instruction returned HTTP 200 with empty content; a normal question on the
   same lane/model returned a real answer minutes later. Do not score 200-empty
   as "lane alive".
5. **Cost caveat (Chris's call)**: 1M-token prompts on the keyed lane consume
   the NVIDIA build-key credits. Promotion routes real traffic at real cost —
   surface plainly, never route around.

## Verdict input for the oracle

The 1m track is **NOT closed — the probe passed on the keyed lane**. Per
DECISION 12097 implementation note (3), the promotion needs a REAL mechanism.
Proposed design for the new DECISION (no unilateral merge):

- **(A) Context-gated long-context pinning (real mechanism):** estimate prompt
  tokens; if >200k, route DIRECTLY to the probe-verified 1M model
  (`nvidia/nemotron-3-super-120b-a12b`, keyed nvidia lane), skipping the race —
  no other lane in the pool can serve >262k (verified: `:free` 400s at 500k).
  ≤200k prompts keep the normal elo race. If the 1M lane's circuit is open,
  fail over to the race as today (never fight the breaker).
- **(B) Model-level quality term:** a small documented constant bonus in
  `candidateScore` for probe-verified models (evidence: this probe's DB rows),
  so the verified lane wins ties among healthy lanes — evidence-backed, not fiat.
- **(C) Status quo:** keep elo-only; long prompts 400/503 today.

Pool-order unshift is NOT proposed (proven no-op in the parallel race).
