{
  "name": "bash-agent-merged",
  "version": "0.1.0",
  "description": "Hyper-merged pure-bash LLM agent kernel from 8 open-source repos",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --hot run src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --target bun --outfile dist/agent.js"
  },
  "dependencies": {
    "glob": "^11.0.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
